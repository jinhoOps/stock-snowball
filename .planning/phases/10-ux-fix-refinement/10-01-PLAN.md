---
phase: 10-ux-fix-refinement
plan: 10-01-PLAN.md
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/common/NumericInput.tsx
  - src/core/SnowballEngine.ts
  - src/core/BacktestEngine.ts
  - src/App.tsx
  - src/components/sections/SimulationControls.tsx
  - src/components/sections/KPIGrid.tsx
  - src/components/common/ShareCard.tsx
  - src/components/common/BigNumberHelper.tsx
autonomous: true
requirements:
  - UI-01
  - UI-05
  - UI-06
  - CORE-01
  - REQ-09-06
must_haves:
  truths:
    - "NumericInput 포커스 시 0이 유지되며 클릭 지점에 커서가 위치한다."
    - "1억 원 이상의 금액 표시 시 1만 원 미만 단위가 절삭되어 출력된다."
    - "KPIGrid와 ShareCard에 '총 납입액' 항목이 표시된다."
    - "USD 입력 필드 하단에 KRW 환산액과 적용 환율 정보가 명시된다."
    - "납입 주기(일/주/월)에 따라 입력값이 해당 주기의 금액으로 엔진에 전달된다."
    - "이미지 저장 버튼 클릭 시 현재 성과 카드가 PNG 이미지로 저장된다."
  artifacts:
    - path: "src/components/common/NumericInput.tsx"
      provides: "Improved focus and cursor behavior"
    - path: "src/core/SnowballEngine.ts"
      provides: "100M KRW truncation logic in formatKoreanWon"
    - path: "src/components/common/ShareCard.tsx"
      provides: "Total contribution display and html-to-image integration"
  key_links:
    - from: "src/App.tsx"
      to: "localStorage"
      via: "useEffect for persistence"
---

<objective>
Phase 10: UX Fix & Refinement 작업을 통해 입력 편의성을 개선하고, 금융 데이터 시각화의 정교함을 높이며, Apple 스타일의 성과 공유 기능을 완성합니다.

주요 목표:
- NumericInput의 고질적인 포커스/선택 동작 개선.
- 대형 금액(1억 이상) 가독성을 위한 자동 절삭 로직 구현.
- 납입 주기(일/주/월) 셀렉터 도입 및 엔진 로직 정합성 확보.
- USD 환산 도우미 및 적용 환율 정보 노출.
- `html-to-image`를 활용한 고품질 성과 공유 카드 저장 기능.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@DESIGN.md
@src/types/finance.ts
@src/core/SnowballEngine.ts
@src/components/common/NumericInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 입력 및 연산 엔진 코어 개선 (Logic & Input Core)</name>
  <files>src/components/common/NumericInput.tsx, src/core/SnowballEngine.ts, src/core/BacktestEngine.ts</files>
  <action>
    - **NumericInput.tsx**: 
      - `handleFocus`에서 `e.target.select()`와 `value === 0`일 때 빈 문자열로 만드는 로직을 제거하여, 포커스 시 값이 유지되고 클릭 지점에 커서가 오도록 수정합니다. (D-01 구현)
    - **SnowballEngine.ts**:
      - `formatKoreanWon` 메서드에 로직 추가: 총액이 1억 원(100,000,000) 이상일 경우, `simplified` 옵션과 관계없이 1만 원 미만 단위를 절삭하여 가독성을 높입니다. (D-02 구현)
      - `simulateRange` 메서드에서 `strategy.baseAmount`를 처리할 때, 현재 하드코딩된 '월간(21.75)' 가정을 제거하고, `params.cycle`에 따라 일/주/월 단위 금액을 그대로 사용하도록 수정합니다. (D-04 엔진 연동)
    - **BacktestEngine.ts**:
      - `run` 메서드 내의 `injectionAmount` 계산 로직에서 `dividedBy(21)` 또는 `dividedBy(4)` 로직을 제거하고, `monthlyInstallment` 필드를 `contributionPerCycle` 개념으로 사용하여 주기에 맞는 금액을 직접 투입하도록 수정합니다.
  </action>
  <verify>
    - `npm test` 실행 시 기존 테스트 통과 확인.
    - `SnowballEngine.test.ts`에 1억 원 이상 절삭 로직에 대한 테스트 케이스 추가 및 검증.
    - NumericInput에 0이 있을 때 클릭 시 0이 사라지지 않고 커서가 유지되는지 확인.
  </verify>
  <done>입력기 동작 개선 및 엔진의 주기별 납입 로직 정합성 확보 완료</done>
</task>

<task type="auto">
  <name>Task 2: UI 확장 및 데이터 영속성 강화 (UI & Persistence)</name>
  <files>src/App.tsx, src/components/sections/SimulationControls.tsx, src/components/common/BigNumberHelper.tsx</files>
  <action>
    - **App.tsx**: 
      - `exchangeRate`와 `currency` 상태를 `localStorage` 캐싱 대상에 추가하여 새로고침 시에도 유지되도록 합니다. (D-07 구현)
      - 시나리오 입력 블록(이름 입력 + 비교군에 추가)을 "저장된 시나리오" 섹션 바로 위로 이동시킵니다.
      - "미래 예측" 텍스트를 "스노우볼"로 변경합니다.
    - **SimulationControls.tsx**:
      - "미래 예측" 탭 레이블을 "스노우볼"로 변경합니다.
      - 납입 주기(일/주/월)를 선택할 수 있는 Segmented Control(Apple 스타일 pill UI)을 추가합니다. (D-04 UI 구현)
      - `NumericInput` 하단에 표시되는 헬퍼 영역을 개선하여, USD 모드일 때 "약 N원 (환율 1,450원 적용)"과 같이 환산액과 적용 환율을 명시합니다. (D-05 구현, DESIGN.md의 Frost Blue 활용)
    - **AdvancedSettingsSheet.tsx**:
      - 참조 자산의 수익률 표기에서 "%/년" 대신 "CAGR"을 사용하도록 변경하고, 시각적 요소를 최소화(색상 제거 등)합니다.
    - **BigNumberHelper.tsx**:
      - `showExchangeRate` 프롭을 추가하여 필요 시 적용 환율 정보를 텍스트로 노출할 수 있도록 확장합니다.
  </action>
  <verify>
    - UI에서 납입 주기를 변경했을 때 결과 차트와 KPI가 즉각 반영되는지 확인.
    - USD 입력 시 하단에 원화 환산액과 환율 정보가 정확히 표시되는지 확인.
    - 새로고침 시 입력값, 환율, 통화 설정이 유지되는지 확인.
  </verify>
  <done>입력 제어 UI 확장 및 실시간 환산/캐싱 기능 구현 완료</done>
</task>

<task type="auto">
  <name>Task 3: 성과 공유 및 시각화 고도화 (Sharing & Polish)</name>
  <files>src/components/sections/KPIGrid.tsx, src/components/common/ShareCard.tsx, src/App.tsx</files>
  <action>
    - **KPIGrid.tsx**: 
      - '총 투자 원금' 카드 외에 '총 납입액(누적 추가 불입액)' 또는 상세 항목을 추가하여 투자 구조를 명확히 보여줍니다. (D-03 구현)
      - CAGR 라벨에 "연복리 수익률" 설명을 보강하고 시각적 강조를 추가합니다.
    - **ShareCard.tsx**:
      - '총 납입액' 항목을 레이아웃에 추가합니다. (D-03 구현)
      - `html-to-image`의 `toPng` 함수를 사용하여 `cardRef`가 가리키는 요소를 이미지로 변환하고 다운로드하는 `handleSaveImage` 함수를 구현합니다. (D-06 구현)
    - **App.tsx**:
      - `KPIGrid`에서 전달되는 `onShare` 콜백을 `ShareCard`의 이미지 저장 로직과 연결합니다.
  </action>
  <verify>
    - KPI Grid에 납입액 항목이 정상 노출되는지 확인.
    - '성과 공유하기' 버튼 클릭 시 Apple 스타일의 카드 이미지가 PNG로 저장되는지 확인.
    - 저장된 이미지의 폰트(SF Pro)와 자간이 `DESIGN.md` 규칙을 준수하는지 육안 확인.
  </verify>
  <done>성과 공유 카드 완성 및 이미지 저장 기능 구현 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → LocalStorage | 사용자 입력 데이터가 브라우저 로컬 저장소에 영속화됨 |
| Client → Image Export | 클라이언트 측에서 `html-to-image`를 통해 DOM을 이미지화함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | LocalStorage | accept | 민감한 금융 정보는 포함되지 않으며, 클라이언트 측 전용 데이터임 |
| T-10-02 | Tampering | html-to-image | mitigate | 생성된 이미지는 읽기 전용이며, 조작 방지를 위해 워터마크(Stock Snowball 로고) 포함 |
| T-10-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for html-to-image |
</threat_model>

<verification>
- 모든 `NumericInput` 요소의 포커스/클릭 동작이 사용자 요구사항을 만족하는가?
- 1억 이상의 대형 금액 표기가 가독성 있게 절삭되는가?
- 일/주/월 납입 주기에 따른 시뮬레이션 결과가 논리적으로 타당한가?
- 성과 공유 이미지가 Apple 디자인 가이드를 준수하며 정상적으로 다운로드되는가?
</verification>

<success_criteria>
- [ ] NumericInput UX 개선 완료 (0 유지, 커서 위치 보존).
- [ ] SnowballEngine 금액 절삭 로직 및 주기별 납입 로직 반영 완료.
- [ ] UI 컴포넌트(KPIGrid, ShareCard) 내 '납입액' 정보 추가 완료.
- [ ] USD 환산 도우미 및 환율 정보 표시 UI 구현 완료.
- [ ] `html-to-image` 기반 성과 이미지 저장 기능 정상 동작.
- [ ] `localStorage`를 통한 실시간 캐싱 정합성 확보.
</success_criteria>

<output>
Create `.planning/phases/10-ux-fix-refinement/10-01-SUMMARY.md` when done
</output>
