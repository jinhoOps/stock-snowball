---
phase: 13-stability-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/common/ShareCard.tsx, src/App.tsx, package.json]
autonomous: true
requirements: [ISSUE-1-1, ISSUE-1-2]

must_haves:
  truths:
    - "공유 이미지 내 납입 주기 레이블과 금액이 줄바꿈 없이 한 줄로 표시됨"
    - "납입 주기(일/주/월) 변경 시 시뮬레이션 결과에 즉각 반영되며 복리 효과가 올바르게 계산됨"
  artifacts:
    - path: "src/components/common/ShareCard.tsx"
      provides: "Fix for share card layout overflow"
    - path: "src/App.tsx"
      provides: "Fix for missing cycle property in simulation calls"
  key_links:
    - from: "App.tsx (activeSimulation)"
      to: "SnowballEngine.simulateRange"
      via: "strategy.cycle"
---

<objective>
GitHub 이슈 #1에서 보고된 안정성 및 로직 버그 해결.
- 공유 이미지 내 납입액 영역의 레이아웃 줄바꿈 오류 수정 (ISSUE-1-1).
- 시뮬레이션 계산 시 납입 주기(cycle) 설정이 누락되는 로직 오류 수정 (ISSUE-1-2).
- 시스템 버전 업데이트 (v1.3.26).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-stability-maintenance/13-CONTEXT.md
@src/components/common/ShareCard.tsx
@src/App.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공유 이미지 레이아웃 수정</name>
  <files>src/components/common/ShareCard.tsx</files>
  <behavior>
    - `ShareCard` 컴포넌트 내의 납입 주기 레이블과 금액 텍스트에 `whitespace-nowrap`을 적용하여 강제 줄바꿈 방지.
  </behavior>
  <action>
    1. src/components/common/ShareCard.tsx 파일에서 `{CYCLE_LABEL[cycle]} 얼마씩?` 텍스트를 포함하는 span에 `whitespace-nowrap` 클래스 추가.
    2. 동일 컴포넌트 내 `SnowballEngine.formatBigNumber`를 호출하는 금액 span에 `whitespace-nowrap` 클래스 추가.
  </action>
  <verify>
    <manual>공유 이미지 생성 시 레이아웃 줄바꿈이 발생하지 않는지 육안으로 확인.</manual>
  </verify>
  <done>공유 이미지의 납입액 영역 레이아웃이 안정적으로 표시됨</done>
</task>

<task type="auto">
  <name>Task 2: 시뮬레이션 납입 주기 반영 로직 수정</name>
  <files>src/App.tsx</files>
  <behavior>
    - `SnowballEngine.simulateRange` 호출 시 `strategy` 객체에 `cycle` 속성을 명시적으로 전달하여 주기에 따른 복리 계산이 수행되도록 함.
  </behavior>
  <action>
    1. src/App.tsx의 `activeSimulation` useMemo 내에서 `strategy` 객체에 `cycle: projectionParams.cycle` 추가.
    2. src/App.tsx의 비교 시나리오 시뮬레이션(`comparingScenarios` useMemo) 루프 내 `SnowballEngine.simulateRange` 호출 시 `strategy` 객체에 `cycle: s.cycle` 추가.
  </action>
  <verify>
    <manual>앱에서 납입 주기를 변경(예: 월 -> 일)했을 때, 총 자산 결과값이 복리 효과에 따라 변하는지 확인.</manual>
  </verify>
  <done>모든 시뮬레이션 호출부에서 납입 주기가 정확히 반영됨</done>
</task>

<task type="auto">
  <name>Task 3: 패키지 버전 업데이트</name>
  <files>package.json</files>
  <action>
    1. package.json의 version 필드를 `1.3.26`으로 업데이트.
  </action>
  <verify>
    <automated>grep '"version": "1.3.26"' package.json</automated>
  </verify>
  <done>프로젝트 버전이 v1.3.26으로 업데이트됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Engine Input | Strategy configuration must include all required properties for correct calculation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure (Calculation Error) | SnowballEngine Callers | mitigate | Ensure all required simulation parameters (cycle, inflation, etc.) are explicitly passed from App state. |
</threat_model>

<verification>
- npm run build (전체 빌드 정합성 확인)
- 납입 주기 변경에 따른 결과값 변화 테스트
- 공유 이미지 레이아웃 육안 검증
- grep '"version": "1.3.26"' package.json
</verification>

<success_criteria>
- 이슈 #1-1 (공유 이미지 레이아웃) 해결 완료.
- 이슈 #1-2 (납입 주기 반영) 해결 완료.
- 버전 v1.3.26 업데이트 완료.
</success_criteria>

<output>
After completion, create .planning/phases/13-stability-maintenance/13-01-SUMMARY.md
</output>
