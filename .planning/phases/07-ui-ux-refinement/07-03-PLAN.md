---
phase: 07-ui-ux-refinement
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [src/components/common/NumericInput.tsx, src/components/sections/SimulationControls.tsx, src/core/__tests__/SnowballEngine.test.ts]
autonomous: true
requirements: [CORE-01, UI-04, UI-06]
must_haves:
  truths:
    - "입력창의 '0' 값이 지워지지 않는 현상이 해결되어 부드럽게 수정 가능함"
    - "슬라이더는 30년까지만 시각적으로 조절 가능하며, 직접 입력으로 그 이상의 기간을 설정할 수 있음"
  artifacts:
    - path: "src/components/common/NumericInput.tsx"
      provides: "개선된 숫자 입력 UX"
---

<objective>
입력창의 '0' 처리 문제를 해결하는 커스텀 입력 컴포넌트를 구현하고, 투자 기간 슬라이더의 시각적 스케일을 30년으로 최적화하여 UX 정밀도를 높입니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ui-ux-refinement/07-CONTEXT.md
@.planning/phases/07-ui-ux-refinement/07-RESEARCH.md
@src/components/sections/SimulationControls.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: NumericInput 컴포넌트 구현</name>
  <files>src/components/common/NumericInput.tsx</files>
  <action>
    기존 input[type=number]의 불편함을 해소하기 위한 래퍼 컴포넌트를 작성합니다:
    - props: value (number), onChange (function), className (string), id (string).
    - 로직:
      1. onFocus 시 값이 0이면 입력창을 비웁니다 (A1).
      2. onBlur 시 입력창이 비어있으면 0으로 복구합니다.
      3. 숫자만 입력 가능하도록 정규식 필터링 적용.
    - 스타일: 기존 SimulationControls의 input 스타일을 계승하거나 디자인 토큰을 사용하십시오.
  </action>
  <verify>
    <automated>ls src/components/common/NumericInput.tsx</automated>
  </verify>
  <done>NumericInput 컴포넌트 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: SimulationControls에 NumericInput 적용 및 슬라이더 스케일 조정</name>
  <files>src/components/sections/SimulationControls.tsx</files>
  <action>
    1. '초기 자산', '납입액' 입력 필드를 NumericInput으로 교체합니다.
    2. 투자 기간 슬라이더의 max 값을 30으로 조정합니다 (per D-03).
    3. 슬라이더는 30년에 고정되더라도 옆의 숫자 입력창(NumericInput)을 통해서는 30년 초과 값을 입력할 수 있도록 상태 로직을 분리하십시오 (Pattern 2).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>SimulationControls UX 최적화 완료</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 금융 연산 및 UX 정밀도 검증 테스트 작성</name>
  <files>src/core/__tests__/SnowballEngine.test.ts</files>
  <behavior>
    - Test 1: KRW <-> USD 전환 시 Decimal.js의 정밀도가 유지되는지 (Banker's Rounding 확인).
    - Test 2: '억/만' 및 'Million/Billion' 포맷팅이 경계값(1억, 10억, 1M, 1B)에서 정확한지.
  </behavior>
  <action>
    SnowballEngine.test.ts에 신규 추가된 통화 및 포맷팅 메서드에 대한 테스트 케이스를 추가하고 실행하십시오.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/SnowballEngine.test.ts</automated>
  </verify>
  <done>금융 무결성 및 포맷팅 검증 테스트 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI Input → State Management | 사용자의 텍스트 입력이 숫자로 파싱되어 애플리케이션 상태로 저장됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | I | NumericInput | mitigate | 비숫자 문자(특수문자, 문자 등)가 상태에 반영되지 않도록 정규식 필터링 강화 |
</threat_model>

<verification>
- 입력창 포커스 시 '0'이 지워지는지 확인.
- 슬라이더를 끝까지 밀었을 때 30년으로 표시되는지, 숫자창에 50을 입력하면 슬라이더는 30에 머무는지 확인.
</verification>

<success_criteria>
- 입력 UX의 사소한 마찰(0 제거 등)이 완전히 해결됨.
- 대규모 투자 기간(30년+) 설정 시에도 UI 일관성 유지.
</success_criteria>

<output>
After completion, create .planning/phases/07-ui-ux-refinement/07-03-SUMMARY.md
</output>
