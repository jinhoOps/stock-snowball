---
phase: 07-ui-ux-refinement
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/components/layout/GlobalNav.tsx, src/components/sections/AdvancedSettingsSheet.tsx, src/components/sections/SimulationControls.tsx]
autonomous: true
requirements: [UI-01, UI-06]
must_haves:
  truths:
    - "헤더 우측에 설정(Gear) 아이콘이 배치되어 고급 설정을 열 수 있음"
    - "고급 설정에서 환율을 직접 수정할 수 있음"
    - "납입 주기에 따라 입력 레이블이 '일/주/월 납입액'으로 동적 변경됨"
  artifacts:
    - path: "src/components/layout/GlobalNav.tsx"
      contains: "Settings icon"
    - path: "src/components/sections/AdvancedSettingsSheet.tsx"
      contains: "exchangeRate"
---

<objective>
고급 설정의 접근성을 개선하고, 환율 설정 및 납입 주기에 따른 동적 레이블 기능을 구현하여 사용자 인터랙션을 정교화합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ui-ux-refinement/07-CONTEXT.md
@.planning/phases/07-ui-ux-refinement/07-RESEARCH.md
@src/components/layout/GlobalNav.tsx
@src/components/sections/AdvancedSettingsSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: GlobalNav에 고급 설정 Gear 아이콘 추가</name>
  <files>src/components/layout/GlobalNav.tsx</files>
  <action>
    - Lucide-react의 'Settings' 또는 'Settings2' 아이콘을 사용합니다.
    - 네비게이션 바 우측 끝에 아이콘 버튼을 배치합니다.
    - 버튼 클릭 시 props로 전달받은 onOpenAdvanced 콜백을 호출하도록 구현하십시오 (per D-04).
    - 스타일: 44x44px 터치 타겟 확보, apple-ink-muted 색상 적용.
  </action>
  <verify>
    <automated>grep -q "Settings" src/components/layout/GlobalNav.tsx</automated>
  </verify>
  <done>GlobalNav 설정 아이콘 추가 및 연결 완료</done>
</task>

<task type="auto">
  <name>Task 2: AdvancedSettingsSheet 설정 항목 확장</name>
  <files>src/components/sections/AdvancedSettingsSheet.tsx</files>
  <action>
    고급 설정 시트에 다음 항목들을 추가합니다 (per D-02, D-04):
    1. 환율(Exchange Rate) 입력 필드: 원/달러 기준 환율 (기본값: 1,450원).
    2. 납입 주기(Contribution Cycle) 선택: '일(Daily)', '주(Weekly)', '월(Monthly)' 옵션을 가진 세그먼트 컨트롤 또는 셀렉트 추가.
    3. 각 상태를 부모 컴포넌트로 전달하기 위한 props 확장 및 연결.
  </action>
  <verify>
    <automated>grep -q "exchangeRate" src/components/sections/AdvancedSettingsSheet.tsx</automated>
  </verify>
  <done>고급 설정 항목(환율, 주기) 추가 완료</done>
</task>

<task type="auto">
  <name>Task 3: SimulationControls 동적 레이블 적용</name>
  <files>src/components/sections/SimulationControls.tsx</files>
  <action>
    - 기존 '고급 설정' 버튼을 삭제하거나 헤더로 이동한 Gear 아이콘이 이를 대체하도록 SimulationControls에서 제거합니다.
    - 선택된 '납입 주기' 상태에 따라 납입액 입력 필드의 레이블을 동적으로 변경합니다 (예: "매일 납입액", "매주 납입액", "매월 납입액").
    - 기본값은 '일(Daily)' 레이블로 설정하십시오.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>동적 레이블 및 UI 최적화 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Advanced Settings → Simulation | 사용자가 수동으로 설정한 환율이 전체 시뮬레이션 연산에 반영됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | I | AdvancedSettingsSheet | mitigate | 환율 입력 시 0 또는 음수 값이 입력되지 않도록 유효성 검사 수행 |
</threat_model>

<verification>
- 상단 Gear 아이콘 클릭 시 고급 설정 시트가 정상적으로 열리는지 확인.
- 고급 설정에서 주기를 'Weekly'로 변경 시 메인 화면의 레이블이 "주 납입액"으로 바뀌는지 확인.
</verification>

<success_criteria>
- Apple 디자인 가이드라인에 따른 정교한 설정 진입점 확보.
- 사용자 설정에 따른 컨텍스트 인지형 UI(동적 레이블) 동작.
</success_criteria>

<output>
After completion, create .planning/phases/07-ui-ux-refinement/07-02-SUMMARY.md
</output>
