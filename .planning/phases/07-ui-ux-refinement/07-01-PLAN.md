---
phase: 07-ui-ux-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/SnowballEngine.ts, src/components/common/BigNumberHelper.tsx, src/components/sections/SimulationControls.tsx]
autonomous: true
requirements: [CORE-04, UI-06]
must_haves:
  truths:
    - "입력창 하단에 '억/만' 또는 'Million/Billion' 단위 가이드가 표시됨"
    - "원/달러 통화 전환 시 금액이 환율에 맞춰 자동 변환됨"
  artifacts:
    - path: "src/components/common/BigNumberHelper.tsx"
      provides: "실시간 단위 가이드 UI"
  key_links:
    - from: "src/components/sections/SimulationControls.tsx"
      to: "src/core/SnowballEngine.ts"
      via: "통화 환산 및 포맷팅 로직 호출"
---

<objective>
사용자가 큰 금액을 입력할 때 인지 부하를 줄여주는 'Big Number Helper'와 원/달러 통화 간의 자동 환산 기능을 구현합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ui-ux-refinement/07-CONTEXT.md
@.planning/phases/07-ui-ux-refinement/07-RESEARCH.md
@src/core/SnowballEngine.ts
@src/components/sections/SimulationControls.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SnowballEngine 통화 환산 및 포맷팅 로직 확장</name>
  <files>src/core/SnowballEngine.ts</files>
  <action>
    SnowballEngine 클래스에 다음 메서드들을 추가합니다 (per RESEARCH.md Pattern 1):
    1. convertCurrency(value, rate, toCurrency): Decimal.js를 사용하여 정밀도를 유지하며 KRW/USD 변환 (KRW는 정수, USD는 소수점 2자리).
    2. formatBigNumber(value, currency): KRW일 경우 '억/만', USD일 경우 'Million/Billion' 단위로 포맷팅하는 로직 구현.
    기존 formatKoreanWon 로직을 재사용하거나 통합하여 일관성을 확보하십시오.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/SnowballEngine.test.ts</automated>
  </verify>
  <done>통화 환산 및 대형 숫자 포맷팅 엔진 메서드 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: BigNumberHelper 컴포넌트 생성</name>
  <files>src/components/common/BigNumberHelper.tsx</files>
  <action>
    입력창 하단에 표시될 Subtle Text 컴포넌트를 생성합니다:
    - props: value (number), currency ('KRW' | 'USD')
    - SnowballEngine.formatBigNumber를 사용하여 텍스트 렌더링.
    - 스타일: 텍스트 크기는 fine-print, 색상은 apple-ink-muted-48, 약간의 상단 마진(mt-1) 적용.
  </action>
  <verify>
    <automated>ls src/components/common/BigNumberHelper.tsx</automated>
  </verify>
  <done>BigNumberHelper 컴포넌트 생성 완료</done>
</task>

<task type="auto">
  <name>Task 3: SimulationControls 통화 전환 및 가이드 통합</name>
  <files>src/components/sections/SimulationControls.tsx</files>
  <action>
    SimulationControls UI에 다음 기능을 통합합니다:
    1. '초기 자산' 및 '납입액' 입력 필드 우측 또는 상단에 통화 전환 토글(KRW/USD) 추가 (per D-02).
    2. 통화 전환 시 현재 입력값을 SnowballEngine.convertCurrency로 환산하여 상태 업데이트.
    3. 각 입력 필드 하단에 BigNumberHelper 배치하여 실시간 가이드 표시 (per D-01).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>SimulationControls 통화 기능 및 가이드 통합 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input → Engine | 사용자 입력값(문자열/숫자)이 금융 연산 엔진으로 전달됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | S | SnowballEngine | mitigate | Decimal.js를 사용하여 부동 소수점 오차 및 오버플로우 방지 |
| T-07-02 | I | BigNumberHelper | mitigate | 잘못된 숫자 형식이 들어올 경우 예외 처리 및 기본값(0) 표시 |
</threat_model>

<verification>
- KRW -> USD -> KRW 전환 시 값이 정수/소수점 규칙에 맞게 복구되는지 확인.
- 1억 이상의 금액 입력 시 '억 만' 단위가 올바르게 표시되는지 확인.
</verification>

<success_criteria>
- 통화 전환 시 자산 시뮬레이션 데이터가 즉각적으로 업데이트됨.
- 사용자가 입력 단위를 시각적으로 즉시 인지할 수 있음.
</success_criteria>

<output>
After completion, create .planning/phases/07-ui-ux-refinement/07-01-SUMMARY.md
</output>
