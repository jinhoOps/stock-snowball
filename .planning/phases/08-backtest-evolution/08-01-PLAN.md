---
phase: 08-backtest-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/finance.ts, src/App.tsx, src/components/sections/SimulationControls.tsx, src/components/common/ScenarioPresetPicker.tsx]
autonomous: true
requirements: [CORE-06, UI-06]

must_haves:
  truths:
    - "Projection and Backtest modes maintain independent parameter sets"
    - "Changing contribution in Projection does not affect Backtest parameters"
    - "Scenario preset picker allows selecting historical dates (Dot-com, GFC, etc.)"
  artifacts:
    - path: "src/types/finance.ts"
      provides: "Unified SimulationParams interface"
    - path: "src/components/common/ScenarioPresetPicker.tsx"
      provides: "Apple-style historical scenario selection"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/sections/SimulationControls.tsx"
      via: "activeParams/updateParams props"
---

<objective>
Decouple simulation states and update the UI to handle mode-specific parameter sets. This prevents "state leakage" and provides a foundation for high-precision backtesting.

Purpose: Prevent unintended side effects when switching modes and prepare for preset historical scenarios.
Output: Refactored state management and mode-aware controls.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-backtest-evolution/08-RESEARCH.md
@.planning/phases/08-backtest-evolution/08-CONTEXT.md
@src/types/finance.ts
@src/App.tsx
@src/components/sections/SimulationControls.tsx
</context>

<tasks>

<task type="auto" id="08-01-01">
  <name>Task 1: Define SimulationParams and Refactor App State</name>
  <files>src/types/finance.ts, src/App.tsx</files>
  <action>
    1. Define `SimulationParams` interface in `src/types/finance.ts` to group {principal, contribution, cycle, assetType, years, rate, accountType, inflationRate, startDate, endDate}.
    2. In `src/App.tsx`, replace individual state variables with two `useState<SimulationParams>` hooks: `projectionParams` and `backtestParams`. (per D-08-01)
    3. Implement a `useMemo` for `activeParams` that switches based on the current `mode` ('PROJECTION' vs 'BACKTEST').
    4. Implement an `updateParams` function that updates the correct state object based on the active mode.
    5. Update `activeSimulation` and `activeBacktest` useMemo hooks to consume these parameter objects.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>State decoupled, App.tsx compiles with grouped parameters</done>
</task>

<task type="auto" id="08-01-02">
  <name>Task 2: Refactor SimulationControls to be Mode-Aware</name>
  <files>src/components/sections/SimulationControls.tsx</files>
  <action>
    1. Update `SimulationControlsProps` to accept `params: SimulationParams`, `onUpdate: (p: Partial<SimulationParams>) => void`, and `mode`.
    2. Refactor all input bindings (NumericInput, ScenarioPresetPicker, Asset Selector) to use `params` and `onUpdate`.
    3. Conditionally render the "Scenario Preset Picker" and "Date Range Inputs" only when `mode === 'BACKTEST'`.
    4. Ensure the currency toggle correctly updates both state objects if necessary, or maintains consistency across modes.
  </action>
  <verify>
    Visual check: Controls correctly reflect active mode's parameters and toggle visibility of backtest-specific inputs.
  </verify>
  <done>Controls are mode-aware and bound to decoupled state</done>
</task>

<task type="auto" id="08-01-03">
  <name>Task 3: Implement ScenarioPresetPicker UI</name>
  <files>src/components/common/ScenarioPresetPicker.tsx</files>
  <action>
    1. Create `ScenarioPresetPicker` component using Apple-style chip layout (horizontal scroll or flex wrap).
    2. Use `Action Blue` for selected states and `Secondary System Background` for unselected.
    3. Define historical presets as defined in 08-RESEARCH.md:
       - "Dot-com Crash": 2000-03-24 to 2002-10-09
       - "Financial Crisis (GFC)": 2007-10-09 to 2009-03-09
       - "COVID-19": 2020-02-19 to 2020-03-23
    4. Clicking a preset should trigger `onUpdate({ startDate, endDate })`.
  </action>
  <verify>
    Visual check: Picker displays presets with correct styling. Clicking updates the date range values in the UI.
  </verify>
  <done>Scenario presets UI is functional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component Props | Mode-specific params passed down; must ensure correct object is sent |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | SimulationControls | mitigate | Validate numeric input ranges in the state update handler |
</threat_model>

<verification>
- Start in PROJECTION mode, set principal to 10M.
- Switch to BACKTEST mode, set principal to 50M.
- Switch back to PROJECTION; principal should still be 10M.
- Verify Preset buttons apply correct date ranges to state.
</verification>

<success_criteria>
1. Projection and Backtest states are fully independent.
2. Historical presets are visible and functional in Backtest mode.
3. UI follows Apple Design Standards (Action Blue, SF font).
</success_criteria>

<output>
After completion, create `.planning/phases/08-backtest-evolution/08-01-SUMMARY.md`
</output>
