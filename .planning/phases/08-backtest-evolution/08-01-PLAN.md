---
phase: 08-backtest-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/finance.ts, src/App.tsx, src/components/sections/SimulationControls.tsx]
autonomous: true
requirements: [CORE-06, UI-06]
REVISED: true

must_haves:
  truths:
    - "Projection and Backtest modes maintain independent parameter sets"
    - "Changing contribution in Projection does not affect Backtest parameters"
    - "Scenario preset picker allows selecting historical dates (Dot-com, GFC, etc.)"
  artifacts:
    - path: "src/types/finance.ts"
      provides: "Unified SimulationParams interface"
    - path: "src/components/common/ScenarioPresetPicker.tsx"
      provides: "Apple-style historical scenario selection"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/sections/SimulationControls.tsx"
      via: "params prop"
---

<objective>
Decouple simulation states and update the UI to handle mode-specific parameter sets. This prevents "state leakage" and provides a foundation for high-precision backtesting.

Purpose: Prevent unintended side effects when switching modes and prepare for preset historical scenarios.
Output: Refactored state management and mode-aware controls.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-backtest-evolution/08-RESEARCH.md
@.planning/phases/08-backtest-evolution/08-CONTEXT.md
@src/types/finance.ts
@src/App.tsx
@src/components/sections/SimulationControls.tsx
</context>

<tasks>

<task type="auto" id="08-01-01" dependency="[]">
  <name>Task 1: Define SimulationParams and Refactor State</name>
  <files>src/types/finance.ts, src/App.tsx</files>
  <action>
    1. Define `SimulationParams` interface in `src/types/finance.ts` to group {principal, contribution, cycle, assetType, years, rate, accountType, inflationRate}.
    2. In `src/App.tsx`, replace individual state variables with two `useState<SimulationParams>` hooks: `projectionParams` and `backtestParams`. (per D-08-01)
    3. Update `activeSimulation` and `activeBacktest` useMemo hooks to consume these parameter objects.
    4. Implement an `activeParams` selector logic that provides the correct params object to sub-components based on `mode`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>State decoupled, App.tsx compiles with grouped parameters</done>
</task>

<task type="auto" id="08-01-02" dependency="['08-01-01']">
  <name>Task 2: Update SimulationControls for Mode-Awareness</name>
  <files>src/components/sections/SimulationControls.tsx</files>
  <action>
    1. Update `SimulationControlsProps` to accept `params: SimulationParams` and a setter.
    2. Refactor inputs to bind to `params.*` values.
    3. Add a placeholder section for "Scenario Presets" that appears only when `mode === 'BACKTEST'`.
    4. Ensure the currency toggle correctly updates both `projectionParams` and `backtestParams` via a unified handler in App.tsx.
  </action>
  <verify>
    Visual check: Controls correctly reflect active mode's parameters.
  </verify>
  <done>Controls are mode-aware and bound to decoupled state</done>
</task>

<task type="auto" id="08-01-03" dependency="['08-01-02']">
  <name>Task 3: Implement ScenarioPresetPicker UI</name>
  <files>src/components/common/ScenarioPresetPicker.tsx, src/components/sections/SimulationControls.tsx</files>
  <action>
    1. Create `ScenarioPresetPicker` component using Apple-style chip layout.
    2. Include presets with verified dates:
       - "Dot-com Crash": 2000-03-24 to 2002-10-09
       - "Financial Crisis (GFC)": 2007-10-09 to 2009-03-09
       - "COVID-19": 2020-02-19 to 2020-03-23
       - "GFC Recovery": 2009-03-10 to Present (or latest data)
       - "April 2025 (Projected)": 2025-04-02 custom start.
    3. Preset data should include `{ name, startDate, endDate, description }` as defined in 08-RESEARCH.md.
    4. Integrate the picker into `SimulationControls.tsx` for the BACKTEST mode.
  </action>
  <verify>
    Visual check: Picker displays all 5 presets with correct styling and dates.
  </verify>
  <done>Scenario presets are selectable in Backtest mode</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component Props | Mode-specific params passed down; must ensure correct object is sent |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | SimulationControls | mitigate | Validate numeric input ranges (0-100B) in state setters |
</threat_model>

<verification>
- Start in PROJECTION mode, set principal to 10M.
- Switch to BACKTEST mode, set principal to 50M.
- Switch back to PROJECTION; principal should still be 10M.
- Verify Preset buttons apply correct date ranges to state.
</verification>

<success_criteria>
1. Projection and Backtest states are fully independent.
2. Historical presets are visible and functional in Backtest mode.
3. UI follows Apple Design Standards (Action Blue, SF font).
</success_criteria>

<output>
After completion, create `.planning/phases/08-backtest-evolution/08-01-SUMMARY.md`
</output>
