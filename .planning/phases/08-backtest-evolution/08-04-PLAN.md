---
phase: 08-backtest-evolution
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified: [src/components/charts/SnowballChart.tsx, src/App.tsx, package.json, .planning/STATE.md]
autonomous: true
requirements: [UI-02, DATA-02]

must_haves:
  truths:
    - "Chart displays future projection range as a transparent 'Cone' using visx Area"
    - "Chart supports Comparison Mode by aligning Projection and Backtest on a relative 'Months Elapsed' axis"
    - "Tooltips display values for pessimistic, average, and optimistic scenarios"
    - "Version is bumped and STATE.md reflects completion"
  artifacts:
    - path: "src/components/charts/SnowballChart.tsx"
      provides: "Range visualization and multi-series comparison"
  key_links:
    - from: "src/components/charts/SnowballChart.tsx"
      to: "src/core/SnowballEngine.ts"
      via: "multi-series data consumer"
---

<objective>
Implement the visual representation of future uncertainty and historical comparisons, finalizing Phase 8 with a high-fidelity charting experience.

Purpose: Emotionalize the data by showing potential ranges and historical survival scenarios.
Output: Upgraded visx chart with Area ranges and comparison overlays.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-backtest-evolution/08-RESEARCH.md
@.planning/phases/08-backtest-evolution/08-CONTEXT.md
@src/components/charts/SnowballChart.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto" id="08-04-01">
  <name>Task 1: Implement Cone of Uncertainty in SnowballChart</name>
  <files>src/components/charts/SnowballChart.tsx</files>
  <action>
    1. Import `Area` from `@visx/shape`.
    2. Add logic to render an `Area` between the `pessimistic` and `optimistic` series. (per D-08-04)
    3. Use `fillOpacity={0.15}` and a color matching the main series.
    4. Update the `Tooltip` to display all three values (Optimistic, Average, Pessimistic) when in Projection mode.
  </action>
  <verify>
    Visual check: A transparent range "cone" appears behind the main projection line.
  </verify>
  <done>Future projection range is visualized</done>
</task>

<task type="auto" id="08-04-02">
  <name>Task 2: Implement Comparison Mode Overlay</name>
  <files>src/components/charts/SnowballChart.tsx, src/App.tsx</files>
  <action>
    1. Add a `comparisonMode` prop to `SnowballChart`.
    2. Implement data transformation that maps both Backtest and Projection data to a shared `monthsElapsed` x-axis. (per D-08-03)
    3. Render the Backtest series as a distinct line (e.g., Orange or Pink) on top of the Projection range.
    4. Update X-axis labels to show "Months" or "Years Elapsed" instead of specific dates when in Comparison Mode.
  </action>
  <verify>
    Visual check: Selecting Comparison Mode overlays the historical performance on top of the future projection range.
  </verify>
  <done>Historical vs Future comparison is functional</done>
</task>

<task type="auto" id="08-04-03">
  <name>Task 3: Version Bump and Project State Update</name>
  <files>package.json, .planning/STATE.md</files>
  <action>
    1. Increment the patch version in `package.json` (e.g., 1.0.35 -> 1.0.36).
    2. Update `.planning/STATE.md` to mark Phase 8 as complete.
    3. Update the "Current Position" and "Todos" in STATE.md.
  </action>
  <verify>
    <automated>grep '"version":' package.json</automated>
  </verify>
  <done>Project metadata updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Chart Rendering | visx components; must handle large arrays without blocking main thread |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Performance | SnowballChart | mitigate | Ensure Area and LinePath components use simplified data points if array length exceeds 10,000 |
</threat_model>

<verification>
- Verify that toggling between modes works smoothly.
- Verify that the Cone of Uncertainty correctly follows the formula (wider over time).
- Verify comparison mode correctly aligns different starting dates to Month 0.
</verification>

<success_criteria>
1. All visualization requirements (Area range, comparison overlay) are met.
2. Tooltips are informative and multi-series aware.
3. The app version is bumped and state reflects completion.
</success_criteria>

<output>
After completion, create `.planning/phases/08-backtest-evolution/08-04-SUMMARY.md`
</output>
