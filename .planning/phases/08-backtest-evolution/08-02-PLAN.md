---
phase: 08-backtest-evolution
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/data/historicalAssets.ts, src/core/SnowballEngine.ts, src/core/__tests__/SnowballEngine.test.ts]
autonomous: true
requirements: [CORE-06, DATA-02]

must_haves:
  truths:
    - "SnowballEngine calculates Median CAGR automatically from historical asset data"
    - "Projection result includes pessimistic, average, and optimistic series"
    - "Variance in projection range increases linearly with years (±0.25% * n)"
    - "Contributions are distributed across 21 business days in the engine"
  artifacts:
    - path: "src/data/historicalAssets.ts"
      provides: "Rolling 1-year Median CAGR utility"
    - path: "src/core/SnowballEngine.ts"
      provides: "Range-aware future projection engine"
  key_links:
    - from: "src/core/SnowballEngine.ts"
      to: "src/data/historicalAssets.ts"
      via: "historical data analysis"
---

<objective>
Enhance the SnowballEngine to provide more realistic future projections by calculating historical median performance and generating a "Cone of Uncertainty" range.

Purpose: Provide users with a range of possible outcomes based on historical volatility.
Output: Upgraded engine with statistical grounding and business-day logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-backtest-evolution/08-RESEARCH.md
@.planning/phases/08-backtest-evolution/08-CONTEXT.md
@src/data/historicalAssets.ts
@src/core/SnowballEngine.ts
</context>

<tasks>

<task type="auto" tdd="true" id="08-02-01">
  <name>Task 1: Implement Median CAGR Logic</name>
  <files>src/data/historicalAssets.ts</files>
  <behavior>
    - Rolling 1-year window (365 points).
    - Calculates (End/Start - 1) for every window.
    - Returns the median of all calculated annual returns.
    - Fallback to 0.1 (10%) if data < 365 points.
  </behavior>
  <action>
    1. Implement `calculateMedianCAGR` function in `src/data/historicalAssets.ts` (per 08-RESEARCH.md Pattern 1).
    2. Add utility to get the median for a specific `AssetType` (e.g., SPY, QQQ).
  </action>
  <verify>
    <automated>npm test src/core/__tests__/Integrity.test.ts</automated>
  </verify>
  <done>Utility for calculating statistical median return is ready</done>
</task>

<task type="auto" tdd="true" id="08-02-02">
  <name>Task 2: Upgrade SnowballEngine for Range Generation</name>
  <files>src/core/SnowballEngine.ts, src/core/__tests__/SnowballEngine.test.ts</files>
  <behavior>
    - Single simulation call returns 3 data series: `average`, `pessimistic`, `optimistic`.
    - At year `n`, pessimistic rate = `median - (0.25% * n)`.
    - At year `n`, optimistic rate = `median + (0.25% * n)`.
    - Base rate is either user-input or the calculated Median CAGR.
  </behavior>
  <action>
    1. Modify `SnowballEngine.simulate` to track 3 state objects (pessimistic, average, optimistic). (per D-08-04)
    2. Implement the time-dependent variance logic: $Expected\_Range = Base\_Rate \pm (0.0025 \times years\_elapsed)$.
    3. Ensure Banker's Rounding is applied to each step of all 3 series.
    4. Update the return type of `simulate` to include all three series.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/SnowballEngine.test.ts</automated>
  </verify>
  <done>Engine generates future projection ranges (Cone of Uncertainty)</done>
</task>

<task type="auto" id="08-02-03">
  <name>Task 3: Refine Daily Contribution with Business Day Logic</name>
  <files>src/core/SnowballEngine.ts</files>
  <action>
    1. Implement `isBusinessDay` helper (skipping Saturdays and Sundays). (per 08-RESEARCH.md Pattern 3)
    2. Update the daily loop: if `isBusinessDay`, invest `monthlyAmount / 21`.
    3. Ensure total invested per month remains accurate (approximately `monthlyAmount`).
  </action>
  <verify>
    <automated>npm test src/core/__tests__/SnowballEngine.test.ts</automated>
  </verify>
  <done>Engine uses 21-business-day distribution for contributions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Data Source | historicalAssets.ts; must handle empty or short data arrays |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Performance | SnowballEngine | mitigate | Optimize loop to calculate all 3 series in a single pass |
</threat_model>

<verification>
- Verify that a 10-year projection with 10% rate results in a range (e.g., ~7.5% to ~12.5% effective rates at the end).
- Verify total investment matches principal + (monthly contribution * months).
</verification>

<success_criteria>
1. Projection range is calculated and follows the ±0.25%*n formula.
2. Median CAGR calculation matches manual spreadsheet verification for SPY/QQQ.
3. Unit tests pass for the multi-series simulation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-backtest-evolution/08-02-SUMMARY.md`
</output>
