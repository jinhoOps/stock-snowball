---
phase: 08-backtest-evolution
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/core/BacktestEngine.ts, src/core/__tests__/BacktestEngine.test.ts]
autonomous: true
requirements: [CORE-04, CORE-06]

must_haves:
  truths:
    - "Backtest calculates wealth accurately for Daily, Weekly, and Monthly cycles"
    - "Buy fees and ISA tax logic are applied correctly within the backtest loop"
    - "Daily contributions in backtest are distributed over 21 business days"
    - "Missing dividends fallback to zero or historical averages without crashing"
  artifacts:
    - path: "src/core/BacktestEngine.ts"
      provides: "High-precision backtesting with tax/fee support"
  key_links:
    - from: "src/core/BacktestEngine.ts"
      to: "src/core/SnowballEngine.ts"
      via: "tax/fee logic parity"
---

<objective>
Upgrade the BacktestEngine to achieve financial parity with the SnowballEngine, supporting multiple cycles, fees, and taxes.

Purpose: Ensure backtest results are realistic and include all major costs.
Output: Enhanced engine logic with full unit test coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-backtest-evolution/08-RESEARCH.md
@.planning/phases/08-backtest-evolution/08-CONTEXT.md
@src/core/BacktestEngine.ts
@src/core/SnowballEngine.ts
</context>

<tasks>

<task type="auto" tdd="true" id="08-03-01">
  <name>Task 1: Add Cycle Support (D/W/M) with Business Day Logic</name>
  <files>src/core/BacktestEngine.ts, src/core/__tests__/BacktestEngine.test.ts</files>
  <behavior>
    - DAILY cycle: Invests `monthlyAmount / 21` on every business day (Mon-Fri).
    - WEEKLY cycle: Invests `monthlyAmount / 4` every 7 calendar days since last investment.
    - MONTHLY cycle: Invests `monthlyAmount` on the first available data point of each new month.
  </behavior>
  <action>
    1. Refactor `BacktestEngine.run` to track `lastInvestmentDate` and `businessDaysThisMonth`. (per D-08-05)
    2. Implement logic to check if current point's date satisfies the cycle condition.
    3. Ensure daily contributions are only made on weekdays.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/BacktestEngine.test.ts</automated>
  </verify>
  <done>Backtest supports D/W/M investment cycles with realistic business day handling</done>
</task>

<task type="auto" tdd="true" id="08-03-02">
  <name>Task 2: Implement Fee and ISA Tax Logic in Backtest</name>
  <files>src/core/BacktestEngine.ts</files>
  <behavior>
    - Buy fees: Deducted from principal and every installment before conversion to shares.
    - ISA Tax: Tracks gains and applies 9.5% tax on everything above the 2M limit at the end of the simulation.
    - Banker's Rounding: Applied to all intermediary monetary calculations.
  </behavior>
  <action>
    1. Port `calculateFees` and `calculateTax` concepts from `SnowballEngine.ts`. (per D-08-02)
    2. Apply `buyFeeRate` to every cash injection.
    3. Implement final tax deduction logic for ISA accounts (9.5% on excess gains).
  </action>
  <verify>
    <automated>npm test src/core/__tests__/BacktestEngine.test.ts</automated>
  </verify>
  <done>Backtest includes fees and taxes in final metrics</done>
</task>

<task type="auto" id="08-03-03">
  <name>Task 3: Dividend Handling and Historical Fallback</name>
  <files>src/core/BacktestEngine.ts</files>
  <action>
    1. Ensure `dividendYield` handling is robust.
    2. If `dividendYield` is missing for a point but exists for the asset generally, use the last known value or 0 as fallback.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/BacktestEngine.test.ts</automated>
  </verify>
  <done>Missing dividends are handled without simulation failure</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Engine Input | Historical data points from local JSON; must handle missing fields |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | BacktestEngine | accept | Financial calculations are client-side only; low risk |
</threat_model>

<verification>
- Run Backtest for SPY with DAILY cycle vs MONTHLY cycle; verify DAILY results in more frequent principal growth.
- Run Backtest with ISA account vs GENERAL; verify final value difference due to tax.
</verification>

<success_criteria>
1. All investment cycles (D/W/M) are supported and tested in backtest.
2. Tax and fee logic matches SnowballEngine outputs for comparable inputs.
3. Unit tests pass for all core engine modifications.
</success_criteria>

<output>
After completion, create `.planning/phases/08-backtest-evolution/08-03-SUMMARY.md`
</output>
