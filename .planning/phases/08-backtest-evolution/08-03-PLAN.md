---
phase: 08-backtest-evolution
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [src/App.tsx, src/components/charts/SnowballChart.tsx, src/components/sections/BacktestView.tsx, package.json, .planning/STATE.md]
autonomous: true
requirements: [DATA-02, UI-03]
REVISED: true

must_haves:
  truths:
    - "Selecting a historical preset automatically updates the backtest date range"
    - "Chart displays 'Comparison Mode' by aligning Projection and Backtest on a 'Months Elapsed' x-axis using visx"
    - "Milestone celebrations trigger when historical wealth goals are reached"
  artifacts:
    - path: "src/components/charts/SnowballChart.tsx"
      provides: "Unified comparison view for disparate time scales"
  key_links:
    - from: "src/components/common/ScenarioPresetPicker.tsx"
      to: "src/App.tsx"
      via: "onSelectPreset callback"
---

<objective>
Implement the 'What-if' logic by connecting presets to the engine and visualizing the comparison between future projections and historical realities using visx.

Purpose: Provide emotional and analytical value by showing how strategies survive past crises.
Output: Integrated comparison UI with synchronized charting and milestone events.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-backtest-evolution/08-RESEARCH.md
@.planning/phases/08-backtest-evolution/08-CONTEXT.md
@src/App.tsx
@src/components/charts/SnowballChart.tsx
</context>

<tasks>

<task type="auto" id="08-03-01" dependency="[]">
  <name>Task 1: Implement Preset Application Logic</name>
  <files>src/App.tsx, src/components/common/ScenarioPresetPicker.tsx</files>
  <action>
    1. Implement `onSelectPreset` in `App.tsx` that updates `backtestParams.startDate` and `endDate` using verified dates from 08-RESEARCH.md.
    2. Ensure the UI responds immediately by re-triggering the `activeBacktest` useMemo.
    3. If the preset is "April 2025", implement a hypothetical logic branch in `BacktestEngine` (or a mock data injector) that simulates the projected 2025 market conditions.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Presets correctly update the backtest simulation parameters</done>
</task>

<task type="auto" id="08-03-02" dependency="['08-03-01']">
  <name>Task 2: Update SnowballChart for Comparison Mode (visx)</name>
  <files>src/components/charts/SnowballChart.tsx</files>
  <action>
    1. Add a `comparisonMode` prop to `SnowballChart`.
    2. When enabled, transform both Projection and Backtest data points into a common format indexed by `monthsElapsed` (0, 1, 2...). (per D-08-03)
    3. Update the **visx** X-axis (`AxisBottom` or `Axis`) to display "Elapsed Months" instead of absolute dates.
    4. Implement **visx** `LinePath` for the second series (Backtest) with a distinct color (e.g., Apple Pink or Orange).
    5. Update the `Tooltip` to show both "Historical Date" and "Projected Date" for the same relative point.
  </action>
  <verify>
    Visual check: Chart overlays Projection line and Backtest line on a single view using visx.
  </verify>
  <done>Chart supports synchronized comparison of different time scales using visx</done>
</task>

<task type="auto" id="08-03-03" dependency="['08-03-02']">
  <name>Task 3: Integrate Milestone Celebrations for Backtest</name>
  <files>src/App.tsx</files>
  <action>
    1. Update the `useEffect` responsible for `confetti` in `App.tsx`.
    2. Ensure it monitors `activeBacktest?.metrics.finalValue` and triggers if a milestone was reached during the historical period.
    3. Add a specialized "Historical Goal Reached" message or animation variant.
  </action>
  <verify>
    Visual check: Confetti triggers when a Backtest simulation ends with a value exceeding a milestone (e.g., 100M).
  </verify>
  <done>Milestones are celebrated in both Projection and Backtest modes</done>
</task>

<task type="auto" id="08-03-04" dependency="['08-03-03']">
  <name>Task 4: Version Bump and State Update</name>
  <files>package.json, .planning/STATE.md</files>
  <action>
    1. Increment the patch version in `package.json` (e.g., 1.0.34 -> 1.0.35).
    2. Update `.planning/STATE.md` to reflect Phase 8 completion, updated metrics, and next steps.
  </action>
  <verify>
    <automated>grep '"version":' package.json</automated>
  </verify>
  <done>Version bumped and project state updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Chart Data Transformation | Data mapped to common x-axis; must handle differing array lengths |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Denial of Service | SnowballChart | mitigate | Ensure data transformation logic is O(n) and throttled for large datasets |
</threat_model>

<verification>
- Select "COVID-19" preset; verify chart shows rapid drop and recovery.
- Toggle "Comparison Mode"; verify Projection (growth) and COVID-19 (real) are overlaid.
- Verify tooltip shows correct relative values.
</verification>

<success_criteria>
1. Historical scenarios are fully integrated and comparable to projections.
2. The UI remains responsive during data transformation for comparison.
3. Milestones provide positive reinforcement for historical strategies.
</success_criteria>

<output>
After completion, create `.planning/phases/08-backtest-evolution/08-03-SUMMARY.md`
</output>
