---
phase: "03-real-world"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/layout/GlobalNav.tsx", "src/core/SnowballEngine.ts", "src/types/finance.ts", "src/data/historicalAssets.ts"]
autonomous: true
requirements: ["UI-06", "CORE-06"]

must_haves:
  truths:
    - "글로벌 네비게이션에서 모든 더미 링크와 아이콘이 제거되고 Snowball 로고만 남습니다."
    - "시뮬레이션 엔진이 단순 고정 이율뿐만 아니라 과거 자산 데이터(QQQM, QLD 등) 기반 연산을 지원합니다."
  artifacts:
    - path: "src/components/layout/GlobalNav.tsx"
      provides: "Minimalist Apple-style header"
    - path: "src/data/historicalAssets.ts"
      provides: "Historical daily return datasets for major indices"
    - path: "src/core/SnowballEngine.ts"
      provides: "Asset-based simulation logic"
  key_links:
    - from: "src/core/SnowballEngine.ts"
      to: "src/data/historicalAssets.ts"
      via: "import and conditional logic"
---

<objective>
Phase 3 UI/UX Refinement & Backtesting Engine (Part 1): Foundation
- GlobalNav를 Apple 미니멀리즘 스타일로 간소화합니다.
- 실제 과거 데이터를 기반으로 한 백테스팅 엔진의 토대를 구축합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-world/3-CONTEXT.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimalist GlobalNav Refactoring</name>
  <files>src/components/layout/GlobalNav.tsx</files>
  <action>
    - `GlobalNav.tsx`에서 `navItems`와 `Search`, `ShoppingBag` 아이콘 및 관련 버튼들을 모두 제거합니다.
    - 좌측의 Snowball 로고(Snowflake 아이콘)만 유지합니다.
    - `backdrop-blur-md`와 `bg-apple-surface-black/80` 스타일이 정확히 적용되었는지 확인합니다 (DESIGN.md 및 context 준수).
    - 로고 클릭 시 홈으로 이동하는 기능은 유지합니다.
  </action>
  <verify>
    <automated>grep -v "Search\|ShoppingBag\|navItems" src/components/layout/GlobalNav.tsx</automated>
  </verify>
  <done>네비게이션 바가 로고만 포함하는 미니멀한 상태로 변경됨.</done>
</task>

<task type="auto">
  <name>Task 2: Backtesting Data & Engine Update</name>
  <files>src/types/finance.ts, src/data/historicalAssets.ts, src/core/SnowballEngine.ts</files>
  <action>
    - `src/types/finance.ts`: `AssetType` (QQQM, QLD, TQQQ, KOSPI) 타입을 추가합니다.
    - `src/data/historicalAssets.ts`: 각 자산별 일간 수익률(daily returns)을 담은 데이터셋을 생성합니다. (현 시점에서는 5~10년치 평균 일간 변동성을 모사한 Mock 데이터 또는 실제 샘플 데이터를 JSON 형태로 구성).
    - `src/core/SnowballEngine.ts`: `simulate` 메서드가 `assetId`를 선택적으로 받도록 수정합니다. `assetId`가 있으면 `annualRate` 대신 데이터셋의 일간 수익률을 사용하여 복리 계산을 수행합니다.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/SnowballEngine.test.ts</automated>
  </verify>
  <done>엔진이 자산 기반 시뮬레이션을 지원하며 데이터셋이 준비됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Engine → UI | 시뮬레이션 결과 데이터가 UI로 전달될 때 소수점 오차가 발생하지 않아야 함 (Decimal.js 사용) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | SnowballEngine | mitigate | Decimal.js를 사용하여 모든 연산의 정밀도를 유지하고 Banker's Rounding 적용 |
</threat_model>

<success_criteria>
- GlobalNav가 미니멀하게 정리됨.
- SnowballEngine이 특정 자산(QQQM 등)을 선택했을 때 고정 이율과 다른 시뮬레이션 결과를 산출함.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-world/03-01-SUMMARY.md`
</output>
