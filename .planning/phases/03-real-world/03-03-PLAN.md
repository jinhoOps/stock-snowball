---
phase: "03-real-world"
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: ["src/App.tsx"]
autonomous: true
requirements: ["UI-06", "CORE-06"]
---

<objective>
Phase 3 UI/UX Refinement & Backtesting Engine (Part 3): Final Integration
- `App.tsx`에서 모든 컴포넌트를 통합하고 3단계 입력 계층 구조를 완성합니다.
- 고급 설정 패널과 백테스팅 엔진을 연결하여 자산별 시뮬레이션을 활성화합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-world/3-CONTEXT.md
@.planning/phases/03-real-world/03-01-PLAN.md
@.planning/phases/03-real-world/03-02-PLAN.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx Integration & State Wiring</name>
  <files>src/App.tsx</files>
  <action>
    - `AdvancedSettingsSheet`를 임포트하고 열림/닫힘 상태(`isOpen`)를 관리합니다.
    - `SimulationControls`에서 "고급 설정" 버튼 클릭 시 `setIsOpen(true)`가 호출되도록 프롭을 전달합니다.
    - 기존 `App.tsx`의 방대한 상태 변수들을 정리하고, `AdvancedSettingsSheet`에 필요한 setter들을 전달합니다.
    - `useMemo`를 통한 시뮬레이션 계산 로직이 새로 추가된 `assetId` 상태를 반영하도록 업데이트합니다.
  </action>
  <verify>
    <automated>grep "AdvancedSettingsSheet" src/App.tsx</automated>
  </verify>
  <done>메인 앱에서 고급 설정 패널이 정상 작동하며 모든 상태가 연동됨.</done>
</task>

<task type="auto">
  <name>Task 2: Asset-based Simulation UX Polishing</name>
  <files>src/App.tsx</files>
  <action>
    - 사용자가 QQQM, QLD 등 특정 자산을 선택했을 때, 고정 수익률 입력창을 비활성화하거나 자산 기반 모드임을 표시합니다.
    - 시뮬레이션 결과(SnowballChart, KPIGrid)가 선택된 자산의 백테스팅 데이터를 기반으로 갱신되는지 확인합니다.
    - 시나리오 저장 시에도 `assetId`가 포함되도록 저장 로직을 보완합니다.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>자산 기반 백테스팅 시뮬레이션 기능이 UI와 엔진 레벨에서 완전히 통합됨.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | App Integration | mitigate | 시뮬레이션 연산이 무겁지 않도록 useMemo를 사용하여 불필요한 재계산 방지 |
</threat_model>

<success_criteria>
- 사용자가 메인 화면에서 금액과 기간만 입력하여 빠르게 결과를 확인 가능함.
- "고급 설정"을 통해 세부 변수 및 특정 자산(QQQM 등)을 선택하여 정교한 백테스팅 결과를 확인 가능함.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-world/03-03-SUMMARY.md`
</output>
