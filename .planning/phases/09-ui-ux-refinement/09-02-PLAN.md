---
phase: 09-ui-ux-refinement
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/components/sections/BacktestView.tsx, src/components/charts/BacktestChart.tsx, src/App.tsx, src/types/finance.ts, src/core/__tests__/Backtesting.test.ts]
autonomous: false
requirements: [REQ-09-03, REQ-09-05]

must_haves:
  truths:
    - "백테스트 모드에서 최대 3개의 자산을 선택하여 동시에 비교할 수 있음"
    - "백테스트 차트에 선택된 모든 자산의 수익률 곡선이 다중 라인으로 표시됨"
    - "각 자산별 수익률과 변동성이 비교 그리드에 표시됨"
  artifacts:
    - path: "src/components/charts/BacktestChart.tsx"
      provides: "Multi-line chart visualization"
    - path: "src/components/sections/BacktestView.tsx"
      provides: "Asset comparison management UI"
  key_links:
    - from: "BacktestView"
      to: "BacktestEngine.run"
      via: "multiple calls for each asset"
---

<objective>
다중 자산 백테스트 비교 기능 구현.
- 단일 자산 백테스트에서 최대 3개 자산 비교 모드로 확장.
- 차트 및 지표 그리드에서 다중 자산 성과를 한눈에 파악할 수 있도록 UI/UX 개선.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-ui-ux-refinement/09-CONTEXT.md
@.planning/phases/09-ui-ux-refinement/09-RESEARCH.md
@src/components/sections/BacktestView.tsx
@src/components/charts/BacktestChart.tsx
@src/App.tsx
@src/core/__tests__/Backtesting.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BacktestView 다중 자산 선택 UI 및 데이터 취합 구현</name>
  <files>src/components/sections/BacktestView.tsx, src/types/finance.ts, src/core/__tests__/Backtesting.test.ts</files>
  <behavior>
    - 최대 3개 자산 선택 가능.
    - 선택된 각 자산에 대해 BacktestEngine이 올바르게 호출되는지 확인.
    - 취합된 결과 데이터가 각 자산 이름과 정확히 매핑되는지 검증.
  </behavior>
  <action>
    1. src/components/sections/BacktestView.tsx에서 selectedAssets: AssetType[] 상태 관리 및 UI 구현.
    2. 각 자산별 백테스트 결과를 취합하여 차트와 그리드에 전달하는 데이터 구조 설계.
    3. src/core/__tests__/Backtesting.test.ts를 활용하여 다중 자산 호출 및 데이터 매핑 로직을 검증하는 테스트 작성.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/Backtesting.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>다중 자산 선택 및 데이터 취합 로직 완료 및 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: BacktestChart 다중 라인 지원</name>
  <files>src/components/charts/BacktestChart.tsx</files>
  <action>
    1. BacktestChart.tsx가 여러 자산의 history 배열을 받도록 props 수정.
    2. visx를 사용하여 각 자산별로 서로 다른 색상의 LinePath를 렌더링.
    3. 범례(Legend) 및 다중 데이터 툴팁 구현.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>차트에 선택된 자산 수만큼 라인이 그려지는지 확인. 툴팁에서 모든 자산의 데이터가 올바르게 표시되는지 확인.</manual>
  </verify>
  <done>다중 라인 백테스트 차트 시각화 완료</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 비교 모드 UX 최종 검증</name>
  <what-built>다중 자산 백테스트 비교 UI 및 차트</what-built>
  <how-to-verify>
    1. 브라우저에서 백테스트 모드로 진입하여 3개 자산(SPY, QQQ, SCHD 등) 선택.
    2. 차트에 3개의 라인이 표시되고 범례와 일치하는지 확인.
    3. 툴팁 스크러빙 시 각 라인의 가치가 정확히 표시되는지 확인.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Asset Selection | Only valid AssetTypes from historicalAssets.ts can be selected. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Resource Exhaustion | BacktestView | mitigate | Limit maximum compared assets to 3 and enforce duration limits from Plan 01. |
</threat_model>

<verification>
- npm test src/core/__tests__/Backtesting.test.ts
- 다중 자산 선택 시 차트 렌더링 정상 여부 및 툴팁 정합성
- 최대 3개 자산 제한 동작 확인
- npm run build (Build integrity)
</verification>

<success_criteria>
- 최대 3개 자산의 성과를 동일 차트와 지표 상에서 비교 가능함.
- 직관적인 다중 라인 시각화 및 범례 제공.
</success_criteria>

<output>
After completion, create .planning/phases/09-ui-ux-refinement/09-02-SUMMARY.md
</output>
