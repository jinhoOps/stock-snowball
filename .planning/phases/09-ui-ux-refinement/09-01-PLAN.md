---
phase: 09-ui-ux-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/BacktestEngine.ts, src/types/finance.ts, src/components/sections/BacktestView.tsx, src/components/sections/SimulationControls.tsx, src/components/sections/AdvancedSettingsSheet.tsx, src/core/__tests__/BacktestEngine.test.ts]
autonomous: true
requirements: [REQ-09-01, REQ-09-02]

must_haves:
  truths:
    - "백테스트 결과에 연율화된 변동성(Volatility)이 표시됨"
    - "투자 기간이 전체 50년, 일간(Daily) 30년으로 제한됨"
    - "시나리오 저장이 복잡한 로직 대신 단순 입력값 유지 방식으로 전환됨"
  artifacts:
    - path: "src/core/BacktestEngine.ts"
      provides: "Annualized Volatility calculation"
    - path: "src/components/sections/SimulationControls.tsx"
      provides: "UI constraints for duration"
    - path: "src/core/__tests__/BacktestEngine.test.ts"
      provides: "Automated tests for metrics and limits"
  key_links:
    - from: "BacktestEngine.run"
      to: "BacktestMetrics.volatility"
      via: "calculation loop"
---

<objective>
금융 엔진 고도화 및 성능 제약 사항 적용.
- BacktestEngine에 연율화된 변동성(Annualized Volatility) 계산 추가.
- 기간 제한(50년, 데일리 30년) 적용으로 브라우저 부하 방지.
- 기존 시나리오 저장 로직 단순화.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-ui-ux-refinement/09-CONTEXT.md
@.planning/phases/09-ui-ux-refinement/09-RESEARCH.md
@src/core/BacktestEngine.ts
@src/types/finance.ts
@src/core/__tests__/BacktestEngine.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BacktestEngine 변동성 계산 추가 및 단위 테스트</name>
  <files>src/core/BacktestEngine.ts, src/types/finance.ts, src/core/__tests__/BacktestEngine.test.ts</files>
  <behavior>
    - BacktestMetrics 인터페이스에 volatility: number 추가.
    - BacktestEngine.run 내부에서 일일 수익률의 표준편차를 구하고 sqrt(252)를 곱해 연율화된 변동성 계산.
    - 변동성 계산 시 MDD와 마찬가지로 자금 투입 영향이 배제된 '단위 가격' 또는 '로그 수익률' 기준 사용.
    - 테스트: 알려진 가격 데이터셋을 사용하여 계산된 변동성 값이 기대값과 일치하는지 확인.
  </behavior>
  <action>
    1. src/types/finance.ts의 BacktestMetrics에 volatility: number 필드 추가.
    2. src/core/BacktestEngine.ts의 run 메서드에서 dailyReturns 배열을 수집하고, 표준편차를 계산하여 annualizedVol 산출 (연구 문서 수식 참조).
    3. src/core/__tests__/BacktestEngine.test.ts에 변동성 계산 검증을 위한 신규 테스트 케이스 추가.
    4. 최종 반환 객체 metrics에 volatility 포함.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/BacktestEngine.test.ts</automated>
  </verify>
  <done>BacktestEngine에서 정확한 연율화 변동성을 계산하여 반환하고 테스트를 통과함</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 기간 제한 UI 및 엔진 적용과 테스트</name>
  <files>src/components/sections/SimulationControls.tsx, src/components/sections/AdvancedSettingsSheet.tsx, src/core/BacktestEngine.ts, src/core/__tests__/BacktestEngine.test.ts</files>
  <behavior>
    - 전체 기본 max: 50, 'DAILY' cycle 시 max: 30 제한.
    - 엔진: 입력값이 제한을 초과할 경우 내부적으로 최대값으로 클리핑(clipping)하거나 경고 발생.
    - 테스트: 60년 또는 DAILY+40년 입력 시 엔진이 이를 안전하게 처리하는지 확인.
  </behavior>
  <action>
    1. src/components/sections/SimulationControls.tsx와 AdvancedSettingsSheet.tsx에서 기간(years) 입력값의 max를 설정 (cycle에 따라 동적 변경).
    2. 입력값 변경 시 유효성 검사 로직 추가하여 제한 초과 시 최대값으로 보정.
    3. src/core/BacktestEngine.ts의 run 메서드 시작 부분에 기간 체크 및 보정 로직 추가.
    4. src/core/__tests__/BacktestEngine.test.ts에 기간 제한 경계값 테스트 케이스 추가.
  </action>
  <verify>
    <automated>npm test src/core/__tests__/BacktestEngine.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>기간 제한이 UI와 엔진 모두에 적용되어 성능 안정성 확보 및 검증 완료</done>
</task>

<task type="auto">
  <name>Task 3: 시나리오 저장 로직 단순화 및 지표 표시</name>
  <files>src/components/sections/BacktestView.tsx, src/App.tsx</files>
  <action>
    1. src/components/sections/BacktestView.tsx의 metrics grid에 '연율화 변동성' 카드 추가.
    2. src/App.tsx에서 handleSaveScenario 로직을 검토하여, 복잡한 DB 동기화 대신 현재 입력 상태를 비교 목록에 즉시 추가하는 가벼운 방식으로 변경.
    3. 기존 "저장하기" 버튼을 "비교군에 추가" 느낌으로 UI 텍스트 및 인터랙션 조정.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>백테스트 뷰에서 변동성 지표가 올바르게 표시됨. 시나리오 저장이 정상적으로 동작하며 비교 목록에 반영됨.</manual>
  </verify>
  <done>지표 표시 고도화 및 시나리오 관리 로직 간소화 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client State | User-input years and cycles are validated before engine execution. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | BacktestEngine | mitigate | Max duration limits (50Y total, 30Y for Daily) enforced at UI and Engine level. |
</threat_model>

<verification>
- npm test src/core/__tests__/BacktestEngine.test.ts (Volatility & Limits)
- UI에서 50년/30년 제한 동작 확인
- 백테스트 뷰 변동성 수치 확인
- npm run build (Build integrity)
</verification>

<success_criteria>
- 연율화된 변동성 지표 계산 및 표시 완료.
- 성능 보호를 위한 기간 제한 적용 완료.
- 시나리오 저장 로직의 복잡성 제거.
</success_criteria>

<output>
After completion, create .planning/phases/09-ui-ux-refinement/09-01-SUMMARY.md
</output>
