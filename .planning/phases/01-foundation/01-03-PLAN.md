---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/components/charts/SnowballChart.tsx]
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "SnowballChart line has a subtle 'Frost Blue' glow effect"
    - "Chart gradient has 'Ice' texture (translucent to transparent)"
    - "Grid lines are minimal or use the 'hairline' color"
  artifacts:
    - path: "src/components/charts/SnowballChart.tsx"
      provides: "Crystal Snowball Chart"
---

<objective>
Advanced Visualization & Interaction Polishing

Purpose: Elevate the SnowballChart to a premium "Crystal Chart" style and ensure all interactions feel like a high-end Apple product.
Output: Upgraded SnowballChart with frost/ice textures and refined grid/axes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/research/PHASE1_DESIGN_REFINEMENT.md
@.planning/research/visualization_strategy.md
@DESIGN.md
@src/components/charts/SnowballChart.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade SnowballChart to Crystal Style</name>
  <files>src/components/charts/SnowballChart.tsx</files>
  <behavior>
    - LinePath must have `filter: drop-shadow(0 0 8px rgba(0, 102, 204, 0.3))`.
    - AreaClosed must use `LinearGradient` with `fromOpacity={0.2}` to `0`.
    - Axes stroke must be `apple-hairline`.
    - Remove vertical grid lines; keep horizontal ones minimal or remove entirely.
  </behavior>
  <action>
    Refactor `SnowballChart.tsx`.
    Apply the glow effect to the line.
    Refine the gradient to feel more "frosty".
    Ensure the chart background container is `bg-apple-canvas` or `bg-apple-surface-pearl` with very soft or no border.
  </action>
  <verify>
    <automated>grep -q "drop-shadow" src/components/charts/SnowballChart.tsx</automated>
  </verify>
  <done>SnowballChart upgraded to premium Crystal style.</done>
</task>

<task type="auto">
  <name>Task 2: Global Animation & Polish</name>
  <files>src/App.tsx, src/components/sections/ProductHero.tsx</files>
  <action>
    - Apply `framer-motion` `LayoutGroup` or `AnimatePresence` for smooth transitions when scenario values change.
    - Ensure `ProductHero` has a clean, airy transition for its title/subtitle.
    - Double check all `tracking` classes are applied to headings.
  </action>
  <verify>
    <automated>grep -q "AnimatePresence" src/App.tsx || grep -q "motion" src/App.tsx</automated>
  </verify>
  <done>Global animation and polish applied.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Denial of Service | Chart Rendering | mitigate | Ensure chart data points are capped or decimated if they exceed 5000+ points to prevent SVG performance issues |
</threat_model>

<verification>
Check chart rendering performance and visual fidelity. Ensure the "Ice" texture feels premium.
</verification>

<success_criteria>
SnowballChart is visually stunning and interactions are buttery smooth.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
