---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/components/layout/GlobalNav.tsx, src/components/sections/KPIGrid.tsx, src/App.tsx, src/components/sections/ProductHero.tsx]
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "GlobalNav has 44px height and Apple-style alignment"
    - "KPICards have 18px rounding and NO shadows"
    - "Buttons use 'rounded-pill' and have scale-down animation on tap"
    - "Input forms are minimal with thin hairlines and SF Pro typography"
  artifacts:
    - path: "src/components/layout/GlobalNav.tsx"
      provides: "Apple Global Nav"
    - path: "src/components/sections/KPIGrid.tsx"
      provides: "Shadow-less KPI Grid"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/sections/KPIGrid.tsx"
      via: "props"
---

<objective>
Core UI Components Refactoring

Purpose: Apply the high-fidelity Apple design patterns to core navigational and informational components.
Output: Refactored GlobalNav, KPIGrid, and Form elements with correct rounding, typography, and motion.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-01-SUMMARY.md
@DESIGN.md
@src/components/layout/GlobalNav.tsx
@src/components/sections/KPIGrid.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor GlobalNav & Logo</name>
  <files>src/components/layout/GlobalNav.tsx</files>
  <behavior>
    - Height MUST be exactly 44px.
    - Background MUST be `bg-apple-surface-black`.
    - Logo should be centered or correctly aligned per DESIGN.md (Apple museum feel).
    - Links must use `text-[12px] tracking-[-0.12px]`.
  </behavior>
  <action>
    Update `GlobalNav.tsx` to match the exact 44px height and typography.
    Replace standard `Apple` icon with a more customized "Snowball Logo" if possible, or ensure it's properly scaled.
    Ensure 44px touch target for all nav items.
  </action>
  <verify>
    <automated>grep -q "h-\[44px\]" src/components/layout/GlobalNav.tsx</automated>
  </verify>
  <done>GlobalNav refactored to Apple standards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor KPIGrid & KPICard</name>
  <files>src/components/sections/KPIGrid.tsx</files>
  <behavior>
    - Remove all `shadow-*` classes from KPICard.
    - Rounding must be `rounded-lg` (18px).
    - Background must be `bg-apple-canvas-parchment`.
    - Add `whileTap={{ scale: 0.96 }}` to cards if they are intended to be interactive.
  </behavior>
  <action>
    Update `KPIGrid.tsx` and its internal `KPICard`. 
    Remove shadows per "Shadow Philosophy".
    Ensure typography uses `text-caption-strong` and `text-body-strong` correctly.
    Apply negative tracking to values.
  </action>
  <verify>
    <automated>grep -v "shadow-" src/components/sections/KPIGrid.tsx | grep -q "rounded-lg"</automated>
  </verify>
  <done>KPIGrid refactored with shadow-less Apple cards.</done>
</task>

<task type="auto">
  <name>Task 3: Apple Style Buttons & Inputs Refactoring</name>
  <files>src/App.tsx</files>
  <action>
    Refactor buttons and inputs in `App.tsx`:
    - Buttons: Use `rounded-pill` for Primary CTA ("저장하기"), `rounded-sm` (8px) for Utility.
    - Add `whileTap={{ scale: 0.95 }}` to all buttons using `framer-motion`.
    - Inputs: Minimalist style, `rounded-sm`, `border-apple-hairline`, focus state with `border-apple-primary`.
    - Update spacing between sections to use `py-section` (80px).
  </action>
  <verify>
    <automated>grep -q "rounded-pill" src/App.tsx && grep -q "whileTap" src/App.tsx</automated>
  </verify>
  <done>Main App UI elements updated to Apple standards.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Input Forms | mitigate | Ensure inputs are sanitized before being passed to SnowballEngine |
</threat_model>

<verification>
Verify that buttons scale down when clicked and that the layout feels "airy" with 80px section padding.
</verification>

<success_criteria>
GlobalNav, KPIGrid, and Buttons 100% compliant with DESIGN.md hierarchy and shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
