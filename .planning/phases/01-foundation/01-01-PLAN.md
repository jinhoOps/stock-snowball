---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tailwind.config.ts, src/index.css]
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "Tailwind theme contains exact tokens from DESIGN.md (colors, typography, spacing)"
    - "SF Pro / Inter font stack is correctly configured with negative tracking"
    - "Global backdrop blur and selection colors are applied"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Apple Design System tokens"
    - path: "src/index.css"
      provides: "Global styles and backdrop-blur utilities"
  key_links:
    - from: "src/main.tsx"
      to: "src/index.css"
      via: "import"
---

<objective>
Design System & Token Sync

Purpose: Establish the foundational design tokens in Tailwind and global CSS to ensure all subsequent UI work adheres to the high-fidelity Apple style defined in DESIGN.md.
Output: Updated Tailwind configuration and global CSS with precise typography and color variables.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/research/PHASE1_DESIGN_REFINEMENT.md
@DESIGN.md
@tailwind.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Tailwind Configuration</name>
  <files>tailwind.config.ts</files>
  <behavior>
    - 'apple' color palette must include: primary (#0066cc), primary-focus, canvas (#ffffff), canvas-parchment (#f5f5f7), surface-tile-1 (#272729), etc.
    - Typography must include: hero (56px, tracking -0.28px), display-lg (40px, tracking 0), display-md (34px, tracking -0.374px), body (17px, tracking -0.374px), etc.
    - Spacing must include: xxs (4px) to section (80px).
    - BorderRadius must include: lg (18px), pill (9999px).
  </behavior>
  <action>
    Modify `tailwind.config.ts` to strictly match the token values in DESIGN.md.
    Ensure `fontFamily` stack for 'display' and 'text' uses `SF Pro` followed by `Inter` and `system-ui`.
    Apply `font-feature-settings: "ss03"` for Inter if possible via CSS or Tailwind utility.
  </action>
  <verify>
    <automated>grep -q "primary: \"#0066cc\"" tailwind.config.ts && grep -q "section: '80px'" tailwind.config.ts</automated>
  </verify>
  <done>Tailwind config updated with all DESIGN.md tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Global CSS</name>
  <files>src/index.css</files>
  <action>
    Update `src/index.css` to include:
    - Custom selection color using `bg-apple-primary/20`.
    - Utility class for `frosted-glass` using `backdrop-filter: blur(20px) saturate(180%)` and `bg-white/80`.
    - Smooth scroll behavior and Apple-style scrollbar (hidden or thin).
    - Font smoothing (`antialiased`).
  </action>
  <verify>
    <automated>grep -q "backdrop-filter" src/index.css</automated>
  </verify>
  <done>Global CSS refined with backdrop-blur and smoothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS Variables | CSS variables are local but exposed to browser; no secrets allowed |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | CSS/Tailwind | accept | Standard behavior; only public design tokens |
</threat_model>

<verification>
Ensure that `npm run build` passes with the new tailwind configuration.
</verification>

<success_criteria>
All design tokens from DESIGN.md are available as Tailwind classes (e.g., `text-hero`, `bg-apple-canvas-parchment`).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
