---
phase: 06-accessibility-refinement
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [
  "src/components/charts/SnowballChart.tsx",
  "src/components/charts/BacktestChart.tsx",
  "src/components/sections/BacktestView.tsx",
  "tailwind.config.ts"
]
autonomous: true
requirements: [UI-01, UI-04, VAL-02]
must_haves:
  truths:
    - "Chart colors are derived from Tailwind theme tokens"
    - "Typography weights match DESIGN.md specifications (semibold for headlines)"
    - "Chart margins align to the 8px grid system"
  artifacts:
    - path: "src/components/charts/BacktestChart.tsx"
      provides: "Tokenized color implementation"
  key_links:
    - from: "src/components/charts/BacktestChart.tsx"
      to: "tailwind.config.ts"
      via: "theme() lookup or CSS variables"
---

<objective>
Refine the design by tokenizing hardcoded values and standardizing typography and layout according to DESIGN.md.

Purpose: Eliminate design debt and ensure visual consistency across the platform.
Output: Tokenized chart styles and standardized typography.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-accessibility-refinement/06-CONTEXT.md
@05-UI-REVIEW.md
@DESIGN.md
@tailwind.config.ts
@src/components/charts/BacktestChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color Tokenization in Charts</name>
  <files>src/components/charts/SnowballChart.tsx, src/components/charts/BacktestChart.tsx</files>
  <action>
    - Replace all hardcoded hex values (per Pillar 3 of UI Review) with Tailwind tokens.
    - Use 'theme("colors.apple.primary")' for #0066cc (Action Blue).
    - Use 'theme("colors.apple.ink-muted-48")' for #7a7a7a or #8e8e93 (Muted Gray).
    - Use 'theme("colors.apple.divider-soft")' for #f0f0f0 (Grid lines).
    - Tip: In visx components, use these in the 'stroke' or 'fill' props, or define them as CSS variables in 'index.css' if needed.
  </action>
  <verify>
    <automated>Select-String -Path src/components/charts/*.tsx -Pattern "#[0-9a-fA-F]{3,6}" -Exclude "*/*" | Measure-Object | Where-Object { $_.Count -eq 0 }</automated>
  </verify>
  <done>No hardcoded hex values remain in chart components.</done>
</task>

<task type="auto">
  <name>Task 2: Typography & Hierarchy Standardization</name>
  <files>src/components/sections/BacktestView.tsx, src/components/charts/SnowballChart.tsx, src/components/charts/BacktestChart.tsx</files>
  <action>
    - Standardize arbitrary font sizes (e.g., text-[10px], text-[11px]) to tokens (per D-03).
    - 'text-[10px]' -> 'text-micro-legal'
    - 'text-[11px]' or 'text-[12px]' -> 'text-fine-print'
    - Update headline and metric weights from 'font-bold' (700) to 'font-semibold' (600) to match Apple's typography style (per D-03).
  </action>
  <verify>
    <automated>grep "font-semibold" src/components/sections/BacktestView.tsx && ! grep "font-bold" src/components/sections/BacktestView.tsx</automated>
  </verify>
  <done>Typography follows the design system tokens and weight guidelines.</done>
</task>

<task type="auto">
  <name>Task 3: Grid Alignment & Margin Cleanup</name>
  <files>src/components/charts/SnowballChart.tsx, src/components/charts/BacktestChart.tsx</files>
  <action>
    - Adjust chart 'margin' objects to use 8px grid multiples (per D-03).
    - Example: top: 60 -> 64, right: 30 -> 32, bottom: 50 -> 48, left: 70 -> 72 or 64.
    - Ensure consistent spacing between chart axes and container edges.
  </action>
  <verify>
    <automated>Check margin values in SnowballChart.tsx and BacktestChart.tsx for 8px alignment.</automated>
  </verify>
  <done>Layout margins are aligned to the 8px base unit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS -> Component | Applying styles from theme |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | Color Tokens | accept | Purely visual tokens, no security impact |
</threat_model>

<verification>
- Inspect elements in the browser to confirm CSS classes (text-fine-print, text-apple-ink-muted-48, etc.) are applied.
- Confirm headlines use font-weight 600.
- Verify chart alignment looks visually "locked" to the grid.
</verification>

<success_criteria>
- 100% color tokenization achieved in target files.
- Typography perfectly aligned with DESIGN.md specifications.
- Consistent 8px grid spacing throughout chart layouts.
</success_criteria>

<output>
After completion, create .planning/phases/06-accessibility-refinement/06-02-SUMMARY.md
</output>
