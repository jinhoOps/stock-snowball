---
phase: 06-accessibility-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/components/sections/SimulationControls.tsx",
  "src/components/sections/AdvancedSettingsSheet.tsx",
  "src/components/charts/SnowballChart.tsx",
  "src/components/charts/BacktestChart.tsx",
  "src/components/common/AnimatedCounter.tsx"
]
autonomous: true
requirements: [UI-01, UI-04, VAL-02]
must_haves:
  truths:
    - "Screen readers can identify input purposes via linked labels"
    - "Interactive chart data is available in a hidden table format"
    - "Counter updates are announced to assistive technology"
    - "Charts can be navigated using keyboard focus"
  artifacts:
    - path: "src/components/charts/SnowballChart.tsx"
      provides: "Hidden data table for screen readers"
    - path: "src/components/common/AnimatedCounter.tsx"
      provides: "Aria-live region for dynamic updates"
  key_links:
    - from: "src/components/sections/SimulationControls.tsx"
      to: "Inputs"
      via: "id/htmlFor linkage"
---

<objective>
Fix accessibility blockers by implementing semantic HTML, ARIA labels, and hidden table fallbacks for charts.

Purpose: Ensure the application is usable by everyone, targeting WCAG AA compliance.
Output: Semantic form controls and screen-reader accessible chart data.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-accessibility-refinement/06-CONTEXT.md
@05-UI-REVIEW.md
@src/components/sections/SimulationControls.tsx
@src/components/charts/SnowballChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic Form Alignment & ARIA Labels</name>
  <files>src/components/sections/SimulationControls.tsx, src/components/sections/AdvancedSettingsSheet.tsx</files>
  <action>
    - Assign unique IDs to all inputs in SimulationControls and AdvancedSettingsSheet.
    - Link corresponding labels using the 'htmlFor' attribute (per D-01).
    - Add 'aria-label' to all buttons, including the mode switcher tabs and "고급 설정" (Advanced Settings) button.
    - Ensure the mode switcher uses 'aria-selected' or 'aria-pressed' to indicate the active state.
  </action>
  <verify>
    <automated>grep "htmlFor=" src/components/sections/SimulationControls.tsx && grep "aria-label=" src/components/sections/SimulationControls.tsx</automated>
  </verify>
  <done>Labels and inputs are semantically linked; buttons have descriptive aria-labels.</done>
</task>

<task type="auto">
  <name>Task 2: Chart Accessibility with Hidden Tables & Keyboard Nav</name>
  <files>src/components/charts/SnowballChart.tsx, src/components/charts/BacktestChart.tsx</files>
  <action>
    - Implement a hidden table fallback (sr-only) within SnowballChart and BacktestChart (per D-02).
    - Limit the table to ~10-12 representative rows (start, milestones, end).
    - Add 'role="img"' and 'aria-label' to the SVG container.
    - Make the chart's interaction region focusable ('tabIndex={0}') and handle arrow key events to simulate scrubbing for keyboard-only users (per D-01).
  </action>
  <verify>
    <automated>grep "tabIndex" src/components/charts/SnowballChart.tsx && grep "sr-only" src/components/charts/SnowballChart.tsx</automated>
  </verify>
  <done>Charts provide a hidden table representation and support keyboard navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Dynamic Update Announcements (Aria-live)</name>
  <files>src/components/common/AnimatedCounter.tsx</files>
  <action>
    - Add 'aria-live="polite"' or 'aria-atomic="true"' to the AnimatedCounter component (per D-04).
    - Ensure that when the value changes, screen readers announce the updated amount without interrupting current speech.
  </action>
  <verify>
    <automated>grep "aria-live" src/components/common/AnimatedCounter.tsx</automated>
  </verify>
  <done>Dynamic KPI updates are announced to assistive technology.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component -> DOM | Rendering accessible attributes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | ARIA attributes | mitigate | Use standard React props; avoid raw DOM manipulation |
</threat_model>

<verification>
- Verify that clicking a label focuses the associated input.
- Check that the "sr-only" table exists in the DOM but is not visible.
- Use a screen reader (or browser accessibility tree) to confirm ARIA labels are present.
- Verify chart can be focused via Tab and interaction triggered via Keyboard.
</verification>

<success_criteria>
- WCAG AA standard labels applied to all interactive elements.
- Chart data accessible via hidden table.
- Keyboard navigation supported for charts and tabs.
- Lighthouse accessibility score significantly improved (Target > 90).
</success_criteria>

<output>
After completion, create .planning/phases/06-accessibility-refinement/06-01-SUMMARY.md
</output>
