---
phase: 05-migrated-from-isf
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified: [src/components/sections/BacktestView.tsx, src/components/charts/BacktestChart.tsx, src/components/sections/SimulationControls.tsx]
autonomous: true
requirements: [UI-05, UI-06]

must_haves:
  truths:
    - "Apple 스타일의 백테스팅 대시보드가 구현됨"
    - "visx를 사용하여 historical data와 principal이 함께 표시되는 차트가 제공됨"
    - "사용자가 Projection과 Backtest 모드를 전환할 수 있는 UI가 존재함"
  artifacts:
    - path: "src/components/sections/BacktestView.tsx"
      provides: "Metrics Grid (CAGR, MDD, IRR) 및 차트 레이아웃"
    - path: "src/components/charts/BacktestChart.tsx"
      provides: "visx 기반의 과거 데이터 시각화 차트"
---

<objective>
백테스팅 결과를 시각화하는 UI를 Apple 스타일 가이드라인(`DESIGN.md`)에 맞게 구현합니다.
기존의 미래 시뮬레이션(Projection) UI와 자연스럽게 전환될 수 있도록 통합합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/migrated-from-ISF/components/BacktestDashboard.tsx
@.planning/migrated-from-ISF/components/AssetChart.tsx
@src/components/sections/SimulationControls.tsx
@src/components/charts/SnowballChart.tsx
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BacktestView 및 Metrics Grid 구현</name>
  <files>src/components/sections/BacktestView.tsx</files>
  <action>
    레거시 `BacktestDashboard`를 참고하여 `BacktestView`를 새로 작성합니다.
    - `DESIGN.md`의 KPI Grid 스타일을 적용합니다.
    - CAGR, IRR, MDD, 최종 자산, 총 투자 원금을 상단 그리드에 배치합니다.
    - `AnimatedCounter`를 사용하여 수치 변화를 부드럽게 표현합니다.
  </action>
  <verify>
    <automated>ls src/components/sections/BacktestView.tsx</automated>
  </verify>
  <done>Apple 스타일 백테스트 대시보드 레이아웃 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: visx 기반 BacktestChart 구현</name>
  <files>src/components/charts/BacktestChart.tsx</files>
  <action>
    `SnowballChart.tsx`의 스타일(얼음 질감, 그라데이션, 스크러빙 인터랙션)을 유지하면서 과거 데이터를 표시하는 `BacktestChart`를 구현합니다.
    - X축은 날짜(Date), Y축은 자산 가치(Value)를 표시합니다.
    - 투자 원금(Principal) 라인을 함께 그려 수익 구간을 명확히 시각화합니다.
    - 스크러빙 시 해당 시점의 날짜와 수익률이 툴팁에 표시되어야 합니다.
  </action>
  <verify>
    <automated>ls src/components/charts/BacktestChart.tsx</automated>
  </verify>
  <done>고정밀 백테스트 차트 구현 완료</done>
</task>

<task type="auto">
  <name>Task 3: 모드 전환 UI 통합</name>
  <files>src/components/sections/SimulationControls.tsx</files>
  <action>
    `SimulationControls` 또는 상단 내비게이션 영역에 'Projection'과 'Backtest' 모드를 전환할 수 있는 세그먼트 컨트롤(Segmented Control)을 추가합니다.
    - 모드 전환 시 `Framer Motion`의 `AnimatePresence`를 사용하여 화면이 부드럽게 교체되도록 합니다.
  </action>
  <verify>
    <automated>grep -i "Backtest" src/components/sections/SimulationControls.tsx</automated>
  </verify>
  <done>시뮬레이션 모드 전환 UI 연동 완료</done>
</task>

</tasks>

<success_criteria>
- 백테스트 대시보드가 `DESIGN.md`의 미니멀리즘과 심미적 가이드를 100% 준수함.
- 차트 인터랙션(스크러빙)이 기존 엔진과 동일한 매끄러움을 유지함.
- 모드 전환이 사용자에게 직관적이며 애니메이션이 끊김 없이 작동함.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migrated-from-isf/05-03-SUMMARY.md`
</output>
