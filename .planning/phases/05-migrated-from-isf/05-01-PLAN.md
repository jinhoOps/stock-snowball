---
phase: 05-migrated-from-isf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/finance.ts, src/core/BacktestEngine.ts, src/core/SnowballEngine.ts]
autonomous: true
requirements: [CORE-06]

must_haves:
  truths:
    - "백테스팅에 필요한 데이터 타입이 finance.ts에 정의됨"
    - "SnowballEngine의 정밀 연산 유틸리티가 BacktestEngine에서 재사용 가능하게 추출됨"
  artifacts:
    - path: "src/types/finance.ts"
      provides: "BacktestParams, BacktestResult, BacktestMetrics 타입"
    - path: "src/core/BacktestEngine.ts"
      provides: "BacktestEngine 클래스 골격 (Decimal.js 기반)"
---

<objective>
이전 프로젝트(ISF)의 레거시 코드를 분석하고, 현재 프로젝트의 고정밀 엔진(`Decimal.js`) 및 타입 시스템에 맞게 인터페이스를 정렬합니다.

핵심 유틸리티(Banker's Rounding 등)를 공유하여 두 엔진 간의 금융적 무결성을 유지합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/migrated-from-ISF/core/types.ts
@.planning/migrated-from-ISF/core/engine.ts
@src/types/finance.ts
@src/core/SnowballEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 시스템 확장</name>
  <files>src/types/finance.ts</files>
  <action>
    `src/types/finance.ts`에 백테스팅을 위한 타입을 추가합니다.
    - `BacktestParams`: `initialPrincipal`, `monthlyInstallment`, `startDate`, `endDate`, `reinvestDividends` 포함.
    - `BacktestMetrics`: `cagr`, `irr`, `mdd`, `totalReturn`, `finalAnnualDividend` 포함.
    - `BacktestResult`: `history` (date, value, principal, isLiquidated) 및 `metrics` 포함.
    - `SimulationMode`: 'PROJECTION' | 'BACKTEST' 타입 추가.
  </action>
  <verify>
    <automated>npx tsc src/types/finance.ts --noEmit</automated>
  </verify>
  <done>백테스팅 관련 타입 정의 완료</done>
</task>

<task type="auto">
  <name>Task 2: 엔진 유틸리티 공유 및 BacktestEngine 골격 생성</name>
  <files>src/core/SnowballEngine.ts, src/core/BacktestEngine.ts</files>
  <action>
    - `SnowballEngine.ts`에서 `bankersRounding` 등 정밀 연산 유틸리티가 잘 export되고 있는지 확인합니다.
    - `src/core/BacktestEngine.ts`를 생성하고 `Decimal.js` 설정을 `SnowballEngine`과 동일하게 적용합니다.
    - `BacktestEngine` 클래스에 `run` 메서드 시그니처를 정의합니다.
  </action>
  <verify>
    <automated>ls src/core/BacktestEngine.ts</automated>
  </verify>
  <done>고정밀 백테스트 엔진 골격 구축 완료</done>
</task>

</tasks>

<success_criteria>
- 백테스팅을 위한 타입 시스템이 현재 아키텍처와 호환됨.
- `BacktestEngine`이 `Decimal.js`를 사용하도록 설정됨.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migrated-from-isf/05-01-SUMMARY.md`
</output>
