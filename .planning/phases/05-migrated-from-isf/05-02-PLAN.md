---
phase: 05-migrated-from-isf
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/core/BacktestEngine.ts, src/core/__tests__/BacktestEngine.test.ts, src/data/historicalAssets.ts]
autonomous: true
requirements: [CORE-06]

must_haves:
  truths:
    - "Decimal.js를 사용한 고정밀 백테스팅 로직이 구현됨"
    - "MDD, CAGR, IRR 지표가 레거시 엔진과 동일하거나 더 정확하게 산출됨"
  artifacts:
    - path: "src/core/BacktestEngine.ts"
      provides: "run(), calculateCAGR(), calculateIRR(), calculateMDD() 구현"
    - path: "src/core/__tests__/BacktestEngine.test.ts"
      provides: "백테스트 엔진 검증 테스트"
---

<objective>
레거시 `BacktestEngine`의 로직을 `Decimal.js` 기반으로 재구현하여 고정밀 백테스팅을 실현합니다.
배당 재투자(TR) 및 자산 청산 로직을 포함하며, MDD, CAGR, IRR 등 핵심 지표를 정확하게 산출합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/migrated-from-ISF/core/engine.ts
@src/core/BacktestEngine.ts
@src/data/indices/qqq.json
@src/data/historicalAssets.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 고정밀 백테스트 핵심 로직 구현</name>
  <files>src/core/BacktestEngine.ts, src/core/__tests__/BacktestEngine.test.ts</files>
  <behavior>
    - 시작일/종료일에 따른 데이터 필터링이 정확해야 함.
    - 매월 적립금 투입 및 수량 매수 로직이 Decimal.js로 계산되어야 함.
    - 배당 수익률 반영 및 재투자가 정확해야 함.
    - 레버리지 하락 등으로 인한 청산 조건(자산 1% 미만)이 작동해야 함.
  </behavior>
  <action>
    레거시 `engine.ts`의 `run` 메서드 로직을 `Decimal.js`를 사용하여 이식합니다.
    - 모든 금액 및 수량 연산은 `Decimal` 타입을 사용합니다.
    - `SnowballEngine.bankersRounding`을 적용하여 연산 오차를 최소화합니다.
  </action>
  <verify>
    <automated>npx vitest run src/core/__tests__/BacktestEngine.test.ts</automated>
  </verify>
  <done>고정밀 백테스트 엔진 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: 백테스트 지표 산출 로직 구현 (MDD, CAGR, IRR)</name>
  <files>src/core/BacktestEngine.ts</files>
  <action>
    - `MDD`: 단위 가격(Unit Price) 추적을 통해 자금 투입 효과를 배제한 순수 자산 낙폭을 계산합니다.
    - `CAGR`: 보유 기간(일수)을 정밀하게 계산하여 연평균 수익률을 산출합니다.
    - `IRR`: 이분법(Bisection method)을 `Decimal.js`로 구현하여 적립식 수익률을 산출합니다.
  </action>
  <verify>
    <automated>npx vitest run src/core/__tests__/BacktestEngine.test.ts</automated>
  </verify>
  <done>핵심 금융 지표 산출 로직 구현 완료</done>
</task>

</tasks>

<success_criteria>
- 백테스트 결과가 레거시 엔진 대비 소수점 오차 없이 일관되게 산출됨.
- MDD 계산 시 추가 불입금이 왜곡을 일으키지 않음.
- IRR 계산이 40회 이상의 반복 시도를 통해 높은 정밀도를 가짐.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migrated-from-isf/05-02-SUMMARY.md`
</output>
