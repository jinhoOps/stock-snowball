---
phase: 05-migrated-from-isf
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified: [src/db/schema.ts, src/hooks/useScenarios.ts, src/App.tsx]
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "RxDB에 백테스트 설정이 저장되고 불러와짐"
    - "시나리오별로 모드(Projection/Backtest)가 영구적으로 보존됨"
    - "백테스트 결과 달성 시 축하 애니메이션이 정상 작동함"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "backtestParams 및 simulationMode 필드 추가"
---

<objective>
백테스팅 설정을 RxDB 시나리오 데이터에 통합하여 사용자가 저장한 시나리오가 모드 정보까지 포함하도록 합니다.
최종적인 UX 마감과 데이터 연동 무결성을 확인합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/db/schema.ts
@src/hooks/useScenarios.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RxDB 스키마 확장 및 마이그레이션</name>
  <files>src/db/schema.ts</files>
  <action>
    RxDB 시나리오 스키마에 `simulationMode`와 `backtestParams` 필드를 추가합니다.
    - `simulationMode`: 'PROJECTION' | 'BACKTEST' (기본값 'PROJECTION')
    - `backtestParams`: 시작일, 종료일, 배당재투자 여부 등을 담는 객체
  </action>
  <verify>
    <automated>grep "simulationMode" src/db/schema.ts</automated>
  </verify>
  <done>데이터 영속성 레이어 확장 완료</done>
</task>

<task type="auto">
  <name>Task 2: useScenarios 훅 업데이트</name>
  <files>src/hooks/useScenarios.ts</files>
  <action>
    `useScenarios` 훅에서 현재 모드에 따라 적절한 엔진(`SnowballEngine` 또는 `BacktestEngine`)을 호출하도록 로직을 업데이트합니다.
    - 선택된 시나리오의 `simulationMode`를 전역 상태 또는 컨텍스트로 노출합니다.
    - 백테스트 실행 시 필요한 데이터 로딩 로직을 통합합니다.
  </action>
  <verify>
    <automated>npx tsc src/hooks/useScenarios.ts --noEmit</automated>
  </verify>
  <done>시나리오 관리 훅 연동 완료</done>
</task>

<task type="auto">
  <name>Task 3: 최종 UX 마감 및 마일스톤 연동</name>
  <files>src/App.tsx</files>
  <action>
    백테스트 결과에서도 자산 목표 달성(예: 원금 2배 달성 시점) 시 기존의 축하 애니메이션(Confetti)이 트리거되도록 연동합니다.
    - 전체 앱의 레이아웃 정합성을 최종 확인합니다.
  </action>
  <verify>
    <automated>grep "Confetti" src/App.tsx</automated>
  </verify>
  <done>최종 통합 및 감성 UI 연동 완료</done>
</task>

</tasks>

<success_criteria>
- 사용자가 백테스트 모드에서 시나리오를 저장하고 나중에 다시 열었을 때 동일한 백테스트 결과가 즉시 표시됨.
- 앱 전반에서 Projection과 Backtest가 통일된 사용자 경험을 제공함.
- 금융 데이터 연동 시 지연 시간이 최소화되며 로딩 상태 처리가 우아함.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migrated-from-isf/05-04-SUMMARY.md`
</output>
