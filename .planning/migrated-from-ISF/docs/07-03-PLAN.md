---
phase: 07-backtest-simulator
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [src/components/backtest/KpiGrid.tsx, src/components/common/Toast.tsx, src/components/backtest/SimulationWarning.tsx]
autonomous: true
requirements: [BACK-05]

must_haves:
  truths:
    - "선택된 시뮬레이션의 CAGR, IRR, MDD 값이 요약 카드에 표시된다."
    - "적금-거치식 등 사용자 오입력 가능성이 있는 시나리오에서 주의 문구가 노출된다."
    - "데이터 로드 완료 또는 계산 성공 시 사용자에게 피드백(Toast)이 제공된다."
  artifacts:
    - path: "src/components/backtest/KpiGrid.tsx"
      provides: "Summary grid for financial metrics"
    - path: "src/components/common/Toast.tsx"
      provides: "Universal feedback UI"
  key_links:
    - from: "src/components/backtest/BacktestDashboard.tsx"
      to: "src/components/backtest/KpiGrid.tsx"
      via: "React props"
---

<objective>
백테스트 결과를 요약하는 KPI 그리드를 구현하고, 토스트 알림 및 경고 시스템을 추가하여 사용자 경험을 완성합니다.
최종 통합 검증을 수행합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backtest-simulator/07-RESEARCH.md
@.planning/phases/07-backtest-simulator/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: KPI 요약 카드 그리드 구현</name>
  <files>src/components/backtest/KpiGrid.tsx</files>
  <action>
    - `calculateLumpSum` 또는 `calculateInstallment` 결과를 받아 CAGR, IRR, MDD, 최종 자산 등을 보여주는 카드형 그리드를 구현합니다. (BACK-05)
    - 수치 변화 시 애니메이션 효과를 적용합니다.
  </action>
  <verify>
    - 설정 변경 시 요약 카드의 수치가 실시간으로 업데이트되는지 확인
  </verify>
  <done>핵심 금융 지표 가시화 완료</done>
</task>

<task type="auto">
  <name>Task 1: 피드백 시스템 및 주의 문구 추가</name>
  <files>src/components/common/Toast.tsx, src/components/backtest/SimulationWarning.tsx</files>
  <action>
    - 재사용 가능한 `Toast` 컴포넌트를 구현합니다.
    - `SimulationWarning` 컴포넌트에서 "적금 거치식 오류" 등 특정 조건 만족 시 경고 문구를 렌더링하도록 처리합니다.
  </action>
  <verify>
    - 시뮬레이션 실행 시 토스트 노출 확인
    - 적금 선택 후 거치식 전환 시 경고 문구 노출 확인
  </verify>
  <done>사용자 가이드 및 피드백 시스템 통합 완료</done>
</task>

<task type="checkpoint:human-verify">
  <name>최종 통합 UAT</name>
  <what-built>데이터 로드 -> 시뮬레이션 계산 -> 차트 및 KPI 업데이트 전 과정</what-built>
  <how-to-verify>
    1. `/apps/step4/index.html` 접속
    2. 자산(QQQ, SPY 등) 선택
    3. 거치식/적립식 전환 및 기간 조절
    4. 상대 비교 모드 테스트
    5. KPI 수치 정합성 최종 확인
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System -> UI | 계산 결과가 사용자에게 시각화됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Repudiation | Result display | accept | 클라이언트 전용 툴이므로 로깅 불필요 |
</threat_model>

<success_criteria>
- 모든 KPI 카드가 올바른 형식을 유지하며 렌더링됨.
- 토스트 및 경고 메시지가 UI 흐름을 방해하지 않으면서 적절히 노출됨.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backtest-simulator/07-03-SUMMARY.md`
</output>
