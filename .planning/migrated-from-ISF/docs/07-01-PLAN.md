---
phase: 07-backtest-simulator
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [public/data/indices/qqq.json, src/core/backtest/engine.ts, src/core/backtest/types.ts, src/core/backtest/engine.test.ts]
autonomous: true
requirements: [BACK-01, BACK-02, BACK-03]

must_haves:
  truths:
    - "정적 데이터 파일을 로드하여 자산 가격 정보를 추출할 수 있다."
    - "거치식 투자 시 CAGR과 누적 수익률이 정확히 계산된다."
    - "적립식 투자 시 IRR과 평단가 가중 수익률이 계산된다."
    - "MDD(최대 낙폭)가 올바르게 산출된다."
  artifacts:
    - path: "src/core/backtest/engine.ts"
      provides: "Backtest simulation logic"
    - path: "public/data/indices/qqq.json"
      provides: "Sample time-series data"
  key_links:
    - from: "src/core/backtest/engine.ts"
      to: "public/data/indices/*.json"
      via: "JSON fetching"
---

<objective>
백테스트 시뮬레이터의 기반이 되는 데이터 구조를 정의하고, 핵심 계산 엔진(CAGR, IRR, MDD 등)을 구현합니다.
TDD 방식을 통해 금융 계산의 정확성을 보장합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backtest-simulator/07-RESEARCH.md
@.planning/phases/07-backtest-simulator/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: 데이터 및 타입 정의</name>
  <files>src/core/backtest/types.ts, public/data/indices/qqq.json</files>
  <action>
    - 백테스트에 필요한 `AssetData`, `SimulationResult`, `SimulationParams` 인터페이스를 정의합니다.
    - `public/data/indices/qqq.json`에 나스닥(QQQ) 20년치 샘플 데이터를 월별 종가 기준으로 생성합니다. (D-02, D-04)
  </action>
  <verify>
    - `src/core/backtest/types.ts` 파일 존재 및 익스포트 확인
    - `public/data/indices/qqq.json` 파일 유효성 확인 (JSON.parse)
  </verify>
  <done>데이터 스키마와 샘플 데이터 적재 완료</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: 시뮬레이션 엔진 구현</name>
  <files>src/core/backtest/engine.ts, src/core/backtest/engine.test.ts</files>
  <behavior>
    - `calculateLumpSum`: 거치식 수익률 및 CAGR 계산 (D-05)
    - `calculateInstallment`: 적립식 수익률 및 IRR 계산 (Newton-Raphson 또는 단순화 공식 사용)
    - `calculateMDD`: 전고점 대비 최대 하락폭 산출
    - `applyDividend`: 배당 재투자(TR) 옵션 적용 로직 (BACK-03)
  </behavior>
  <action>
    - `src/core/backtest/engine.ts`에 핵심 로직을 작성합니다.
    - Vitest(또는 현재 환경의 테스트 도구)를 사용하여 극단적 케이스 및 알려진 수익률 데이터를 바탕으로 검증합니다.
  </action>
  <verify>
    <automated>npm test src/core/backtest/engine.test.ts</automated>
  </verify>
  <done>정확성이 검증된 시뮬레이션 엔진 엔진 확보</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Data -> Engine | JSON 파일의 수치 데이터가 엔진의 계산 로직에 입력됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | public/data/*.json | accept | 정적 파일이므로 빌드 타임 무결성 확인으로 충분 |
| T-07-02 | Information Disclosure | Financial logic | accept | 모든 로직은 클라이언트 사이드에서 공개적으로 수행됨 |
</threat_model>

<success_criteria>
- 거치식/적립식 시뮬레이션 결과가 수동 계산값과 일치함.
- MDD 계산이 하락장 데이터를 포함한 샘플에서 올바르게 동작함.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backtest-simulator/07-01-SUMMARY.md`
</output>
