---
phase: 07-backtest-simulator
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [apps/step4/index.html, src/entries/step4.tsx, src/components/backtest/BacktestDashboard.tsx, src/components/backtest/AssetChart.tsx]
autonomous: true
requirements: [BACK-04]

must_haves:
  truths:
    - "사용자가 브라우저에서 Step 4 진입 시 백테스트 대시보드가 표시된다."
    - "자산 선택 및 투자 설정(거치/적립) 변경 시 차트가 즉시 업데이트된다."
    - "상대 비교 모드 활성화 시 기준 자산이 0% 라인으로 평탄화된다."
  artifacts:
    - path: "src/entries/step4.tsx"
      provides: "React entry point for Step 4"
    - path: "src/components/backtest/AssetChart.tsx"
      provides: "SVG/Canvas based responsive chart"
  key_links:
    - from: "src/entries/step4.tsx"
      to: "src/components/backtest/BacktestDashboard.tsx"
      via: "React rendering"
---

<objective>
React를 사용하여 백테스트 시뮬레이터의 UI 레이아웃을 구성하고, 동적인 수익률 차트 컴포넌트를 구현합니다.
상대 비교 모드(Relative Comparison) 기능을 포함합니다.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backtest-simulator/07-RESEARCH.md
@.planning/phases/07-backtest-simulator/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Step 4 진입점 및 레이아웃 설정</name>
  <files>apps/step4/index.html, src/entries/step4.tsx, src/components/backtest/BacktestDashboard.tsx</files>
  <action>
    - `apps/step4/index.html`을 생성하고 Vite 엔트리 포인트를 연결합니다.
    - `src/entries/step4.tsx`에서 React Root를 초기화합니다.
    - `BacktestDashboard.tsx`에서 사이드바(설정 영역)와 메인 콘텐츠(차트 영역)를 포함한 기본 레이아웃을 Tailwind CSS로 구성합니다. (D-03)
  </action>
  <verify>
    - `npm run dev` 실행 후 `/apps/step4/index.html` 접속 시 빈 레이아웃 노출 확인
  </verify>
  <done>Step 4 React 인프라 및 대시보드 구조 완성</done>
</task>

<task type="auto">
  <name>Task 1: 반응형 수익률 차트 컴포넌트 구현</name>
  <files>src/components/backtest/AssetChart.tsx</files>
  <action>
    - SVG를 기반으로 여러 자산의 수익률 곡선을 그리는 `AssetChart` 컴포넌트를 작성합니다.
    - `relativeMode` prop에 따라 특정 자산을 0%로 평탄화하는 좌표 변환 로직을 구현합니다. (BACK-04)
    - 툴팁 및 호버 인터랙션을 추가합니다.
  </action>
  <verify>
    - 더미 데이터를 사용하여 차트 렌더링 및 모드 전환(Absolute vs Relative) 기능 확인
  </verify>
  <done>동적 인터랙션이 포함된 백테스트 전용 차트 구현 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input -> State | 설정 변경값이 React 상태로 관리됨 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | Chart Rendering | mitigate | 대량 데이터 렌더링 시 `useMemo` 및 Canvas 최적화 고려 |
</threat_model>

<success_criteria>
- 차트가 브라우저 리사이즈에 맞춰 반응형으로 동작함.
- 상대 비교 모드 전환 시 Y축 스케일이 % 단위로 적절히 변경됨.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backtest-simulator/07-02-SUMMARY.md`
</output>
