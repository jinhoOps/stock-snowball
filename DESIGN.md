# Stock Snowball 디자인 규칙

Stock Snowball은 적립식 투자 시뮬레이션, 과거 데이터 백테스트, 시나리오 저장·비교를 위한 도구다. 사용자는 입력 조건을 이해하고 결과의 금액·기간·수익률을 비교할 수 있어야 한다. Frost Blue, 밝은 배경, 둥근 컨트롤을 유지하며 데이터와 조작의 우선순위를 명확하게 표현한다.

이 문서는 현재 제품의 디자인 계약이다. 스타일 값은 `src/index.css`의 CSS 변수와 `tailwind.config.ts`에서, 반복되는 표현은 공통 컴포넌트와 `src/styles/components.css`에서 관리한다. 새 값을 추가하기 전에 기존 토큰과 컴포넌트로 표현할 수 있는지 확인한다.

## 색상

RGB 채널 값의 원본은 `src/index.css`의 `:root`에 둔다. Tailwind는 `rgb(var(--color-…) / <alpha-value>)`로 참조하여 `bg-apple-primary/10`, `border-apple-error/20` 같은 투명도 변형을 지원한다. 컴포넌트마다 같은 색의 HEX 값을 복사하지 않는다.

기존 `apple-surface-chip-translucent`는 기본 불투명도 64%를 유지하고 `/80` 등의 명시적 변형도 지원한다.

| 용도 | Tailwind 토큰 | 기준 색 |
| --- | --- | --- |
| 주요 동작·선택 상태 | `apple-primary` | Frost Blue `#0066cc` |
| 주요 동작 hover | `apple-primary-focus` | `#005bb5` |
| 어두운 배경의 브랜드 포인트 | `apple-primary-on-dark` | `#2997ff` |
| 제목·본문 | `apple-ink`, `apple-body` | `#1d1d1f` |
| 보조 설명·축 레이블 | `apple-secondary` | `#626268` |
| 강한 보조 텍스트 | `apple-ink-muted-80` | `#333333` |
| 오류·삭제·음수 결과 | `apple-error` | `#b42318` |
| 성공·양수 결과 | `apple-success` | `#18794e` |
| 페이지·입력 바탕 | `apple-canvas` | `#f0f2f5` |
| 중립 컨트롤 바탕 | `apple-canvas-parchment` | `#f5f5f7` |
| 카드·패널 | `apple-surface-pearl` | `#fafafc` |
| 흰 표면 | `apple-surface-white` | `#ffffff` |
| 어두운 내비게이션 표면 | `apple-surface-black` | `#000000` |
| 구분선 | `apple-hairline`, `apple-divider-soft` | `#e0e0e0`, `#f0f0f0` |
| 주요 버튼 글자 | `apple-on-primary` | 흰색 |
| 어두운 표면 위 글자 | `apple-on-dark` | `#f0f2f5` |

`apple-ink-muted-48`과 `apple-ink-muted-64`는 기존 화면을 위한 `apple-secondary` 별칭이다. 이름의 숫자를 투명도로 해석하지 않는다. 새 보조 설명에는 `apple-secondary`를 우선한다. `apple-gray`, `apple-ink-muted`처럼 정의되지 않은 이름을 만들지 않는다.

브랜드, 상태, 차트 색상은 역할을 구분한다. 파란색은 조작과 선택 상태에 사용하며, 오류·성공 색상에는 설명·부호·아이콘을 함께 제공한다. 차트는 여러 데이터 계열을 구별해야 하므로 전용 색상과 선 패턴을 유지할 수 있다. 같은 시나리오의 색은 범례·선·상세 정보에서 일치시킨다.

SVG에서 쓰는 `--apple-primary`, `--apple-ink`, `--apple-ink-muted-48`, `--apple-gray-400`, `--apple-divider-soft`, `--apple-hairline`과 시장선 `--market-*`는 `rgb(var(--color-…))` 형태의 완전한 색이다. RGB 채널만 전달하여 SVG의 `stroke`·`fill`을 무효화하지 않는다. 시장 지수·이동평균 색상은 `--color-market-*`가 관리하고 시나리오 계열은 차트의 기존 색상 매핑을 따른다.

## 레이아웃과 간격

| 토큰 | 값 | 적용 |
| --- | --- | --- |
| `max-w-content` | 1000px | 기본 시뮬레이션, 입력, 시나리오, 내비게이션 |
| `max-w-analysis` | 1200px | 여러 계열·지표를 나란히 읽는 백테스트 분석 |
| `min-h-control` | 44px | 모든 조작 가능한 버튼·선택 항목 |
| `h-field` | 48px | 숫자·날짜·텍스트 입력과 select |
| `rounded-card` | 18px | 결과 카드·설정 그룹·분석 패널 |
| `rounded-pill` | 9999px | 버튼·입력·선택 컨트롤 |
| `z-nav` | 40 | 고정 내비게이션 |
| `z-overlay` | 60 | 모달 배경 |
| `z-dialog` | 70 | 모달 내용 |

페이지는 가운데 정렬하고 좌우 gutter를 모바일 16px, 넓은 화면 24px로 맞춘다. 바깥 레이아웃이 gutter를 소유하며 같은 구간의 자식에 페이지 gutter를 반복하지 않는다. 백테스트의 1200px 폭은 분석 밀도를 위한 의도적인 예외다. 기본 시뮬레이션과 저장 시나리오는 1000px를 따른다.

간격은 4px 단위를 우선한다. 컨트롤 사이에는 8–12px, 관련 입력 묶음 사이에는 16–24px, 큰 섹션 사이에는 32–48px를 사용한다. 기존 이름 `xxs=4`, `xs=8`, `sm=12`, `md=17`, `lg=24`, `xl=32`, `xxl=48`, `section=80`은 유지하지만 새 묶음의 간격은 역할에 맞는 기본 Tailwind 간격으로 선택한다. 모바일에서는 열을 쌓고, 넓은 화면에서만 여러 열로 펼친다.

긴 시나리오 이름과 큰 금액을 넣어도 페이지에 가로 스크롤이 생기지 않아야 한다. 줄바꿈 가능한 텍스트에는 충분한 공간을 주고, 의도적으로 생략한 이름은 접근 가능한 전체 이름을 보존한다. 넓은 표는 자신의 영역 안에서만 스크롤한다.

## 글자와 숫자

제목과 큰 숫자는 `font-display`, 본문과 입력은 `font-text`를 사용한다. 폰트는 SF Pro, Inter, 시스템 폰트 순으로 대체하며 다운로드를 전제하지 않는다.

| 토큰 | 크기 / 줄 높이 / 굵기 | 용도 |
| --- | --- | --- |
| `text-hero` | 56px / 1.07 / 600 | 큰 화면의 대표 제목 |
| `text-display-lg` | 40px / 1.1 / 600 | 대표 결과·히어로 |
| `text-display-md` | 34px / 1.47 / 600 | 기존 대형 제목 |
| `text-display-sm` | 28px / 1.2 / 600 | 주요 섹션 제목 |
| `text-title-md` | 24px / 1.25 / 600 | 공통 지표 값·설정 제목 |
| `text-title-sm` | 20px / 1.3 / 600 | 분석 패널 제목 |
| `text-body` | 17px / 1.47 / 400 | 본문·입력 |
| `text-body-strong` | 17px / 1.24 / 600 | 강조 문장 |
| `text-caption` | 14px / 1.43 / 400 | 설명·보조 정보 |
| `text-caption-strong` | 14px / 1.29 / 600 | 필드·지표 레이블 |
| `text-fine-print` | 12px / 1 / 400 | 짧은 부가 정보 |

기존 `lead`, `lead-airy`, `tagline`, `dense-link`, `button-large`, `button-utility`, `micro-legal`, `nav-link` 토큰도 유지한다. 새 화면의 핵심 설명을 작은 글자로 축소하여 공간 문제를 해결하지 않는다. 여러 줄 안내에 `text-fine-print`를 쓴다면 `leading-relaxed` 등으로 행간을 확보한다.

금액과 비율은 단위를 분명히 구분한다. 금액만 통화 포맷터와 큰 수 보조 표기를 사용하고, 수익률·비중 등의 백분율에 원화·달러 보조 문구를 붙이지 않는다. 양수·음수 수익률은 부호를 보존하며 색만으로 뜻을 전달하지 않는다. 소수점 자리와 숫자 포맷은 기존 계산·포맷 함수의 계약을 따른다.

## 공통 컴포넌트 계약

각 화면은 계산·저장·임시 입력 상태를 소유하고, 공통 컴포넌트는 시각적 표현과 접근성 연결을 담당한다.

| 컴포넌트 | API | 책임 |
| --- | --- | --- |
| `Button` | 네이티브 button props, `variant="primary\|secondary\|ghost\|danger"`, `size="default\|compact\|icon"` | 동작 우선순위, 크기, focus·disabled 상태 |
| `SegmentedControl` | `label`, `value`, `options`, `onChange`, `size="default\|compact"`, `fullWidth` | 이름이 있는 단일 선택 그룹과 선택 상태 |
| `Field` | `label`, `htmlFor`, `hint?`, `error?`, 단일 입력 `children`, `className?` | 입력 레이블과 도움말·오류 연결 |
| `Surface` | `padding="default\|compact\|none"`, `children`, HTML 속성 | 카드 표면·테두리·반경·내부 간격 |
| `Notice` | `tone="info\|error"`, `children`, HTML 속성 | 안내와 오류의 일관된 표시 |
| `MetricCard` | `label`, `value`, `detail?`, `badge?`, `highlighted?`, `children?`, HTML 속성 | 지표 레이블, 값, 표면의 공통 구조 |
| `Sheet` | `open`, `onClose`, `title`, `children`, `footer?` | 포털·모달 이름·focus·스크롤·닫기 동작 |

### 버튼과 선택

`primary`는 적용·저장 등 주요 동작, `secondary`는 보조 동작, `ghost`는 닫기·세부 조작, `danger`는 삭제에 사용한다. 주요 버튼과 선택된 항목은 Frost Blue 바탕과 흰색 글자로 통일한다. 같은 영역에 주요 동작을 과도하게 반복하지 않는다.

`compact`는 가로 padding만 줄인다. 모든 크기의 조작 높이는 최소 44px이며, `icon`은 최소 44×44px다. 아이콘만 있는 버튼은 `aria-label`로 동작 이름을 제공한다. 작은 글자 크기를 작은 클릭 영역의 근거로 삼지 않는다.

`SegmentedControl`은 `label`로 그룹 이름을 제공하고 각 버튼에 `aria-pressed`로 선택 여부를 알린다. 통화, 납입 주기, 계좌, 표시 기준 등의 단일 선택은 같은 컴포넌트를 사용한다. 사용할 수 없는 옵션은 비활성 상태와 이유를 함께 제공한다. 긴 옵션 이름은 읽을 수 있게 배치하고 모바일에서도 각 선택 영역을 유지한다.

### 필드와 표면

`Field`는 `htmlFor`를 자식 입력의 `id`로 연결하고, 도움말·오류를 `aria-describedby`에 연결한다. 오류가 있으면 `aria-invalid`를 전달한다. 자식은 단일 input·select 또는 이러한 네이티브 속성을 전달하는 커스텀 입력이어야 한다. 같은 모듈의 `Input`·`Select`가 입력 스타일을 공유한다. 날짜에는 시작·종료를 구분하는 이름을, 시나리오 이름 입력에는 저장할 대상이 드러나는 레이블을 제공한다. placeholder는 레이블을 대신하지 않는다.

입력 높이는 48px로 통일한다. 숫자 입력의 정규화·단위·최솟값·최댓값은 기존 동작을 보존한다. 날짜 범위 오류나 데이터 부족은 입력 가까이에 `Notice`로 설명하고 사용 가능한 다음 동작을 제공한다.

`Surface`는 `rounded-card`, hairline 테두리, pearl 바탕을 기본으로 한다. 기본 padding은 `p-5 sm:p-6`, compact는 `p-4`, none은 `p-0`이다. 카드 그림자가 필요하면 `shadow-card`처럼 약하게 사용한다. frosted blur는 내비게이션·모달처럼 뒤 화면과 겹치는 표면에 우선 적용한다.

### 지표와 시나리오

`MetricCard`는 레이블, 주요 값, 보조 정보 순으로 읽힌다. `dt`·`dd`를 포함하므로 지표 묶음은 `dl` 안에 놓는다. 기본 값은 `text-title-md`를 사용하고 강조 지표만 브랜드 색을 사용한다. 시뮬레이션 KPI와 백테스트 KPI는 같은 표면·레이블·값의 위계를 공유하며 각 계산식과 애니메이션 훅은 유지한다.

저장 시나리오의 선택, 비교 포함·제외, 삭제는 각각 이름이 있는 독립된 조작이다. 클릭 가능한 카드 안에 다른 버튼을 중첩하지 않는다. 현재 선택과 비교 상태는 키보드와 보조 기술에서도 구분할 수 있어야 한다.

### 백테스트 기간과 경제 위기 시나리오

`DateRangeControl`은 현재 시작일·종료일을 하나의 버튼에 표시하고 공통 `Sheet` 안에서 편집한다. 연도·월 바로 이동, 달력 범위 선택, `YYYY-MM-DD` 또는 숫자 8자리 입력을 제공한다. 날짜 셀은 좁은 모바일에서도 44px 이상이다. 편집 중에는 계산 결과를 바꾸지 않고, 유효한 시작일·종료일이 완성된 뒤 `기간 적용`으로 함께 갱신한다. 취소·Escape는 변경을 버리고 열기 버튼으로 focus를 복원한다.

최근 1·3·5·10년과 전체 기간은 선택 자산의 마지막 공통 데이터 날짜를 기준으로 한다. `HistoricalScenarioPicker`는 제목과 펼치기 버튼을 항상 표시하고 기본 상태는 접힘이다. 선택된 사건은 접힌 상태에서도 이름과 기간을 표시하며, 레버리지 가족 비교에서도 유지한다. 이름·날짜·관찰 범위를 함께 보여주고 선택 상태를 `aria-pressed`로 전달한다. 지원하지 않는 시나리오도 숨기지 않고 공통 데이터 범위와 계산 불가 이유를 표시한다. 일부 기간만 잘라 원래 사건 이름으로 적용하거나 존재하지 않는 과거 가격을 생성하지 않는다.

### 설정 Sheet

`Sheet`는 `document.body`로 포털을 렌더링하고 `role="dialog"`, `aria-modal`, 제목 연결을 제공한다. 열릴 때 모달 안으로 focus를 옮기고 Tab·Shift+Tab을 모달 안에 유지한다. Escape와 닫기 버튼으로 닫으며, 닫힌 뒤에는 열었던 컨트롤로 focus를 돌려준다. 배경은 상호작용할 수 없게 하고 원래 body 스크롤 상태를 복원한다.

모달 배경과 내용은 각각 `z-overlay`, `z-dialog`를 사용하여 내비게이션보다 위에 놓인다. 모바일은 하단 Sheet, 넓은 화면은 오른쪽 패널을 사용한다. 제목·닫기 버튼은 내용 스크롤에 가려지지 않고, 긴 내용과 footer는 작은 화면에서도 접근 가능해야 한다.

고급 설정은 화면이 임시 편집 상태를 소유한다. 취소·닫기는 미적용 변경을 버리고, 적용만 실제 시뮬레이션 설정을 갱신한다. 공통 Sheet 내부에 금융 계산이나 설정 저장 로직을 넣지 않는다.

## 공유 이미지의 밀도 예외

`ShareCard`의 이미지 내보내기 영역은 정해진 이미지 폭 안에서 금액·조건·브랜드를 배치하므로 작은 레이블과 조밀한 간격을 사용할 수 있다. 이 영역은 정적인 결과 이미지이며, 화면의 입력·선택·닫기·저장 버튼에는 이 예외를 적용하지 않는다. 제목은 최대 두 줄로 제한하고, 짧은 금액·단위·날짜는 한 덩어리로 유지한다. 내보낸 PNG를 직접 열어 텍스트 겹침·잘림과 수익률 부호를 확인한다.

## 상태와 검증

hover, focus-visible, pressed, disabled 상태를 함께 설계한다. 키보드 focus는 배경과 구별되어야 하고, disabled의 이유는 필요한 곳에서 읽을 수 있어야 한다. 상태 변경에 따라 중요 안내가 나타나면 적절한 status·alert 의미를 제공한다.

`prefers-reduced-motion`에서는 움직임을 줄이거나 제거하고 최종 숫자·선택 상태는 그대로 보여준다. 애니메이션을 기다려야만 읽을 수 있는 결과를 만들지 않는다.

- `npm run design:check`: 실제 Tailwind CSS를 생성하여 앱의 `src/**/*.ts`·`src/**/*.tsx`에 쓰인 토큰 유틸리티가 생성되는지 확인한다. 테스트 파일은 제외하고 반응형·상태·투명도 변형을 함께 확인한다.
- `npm run design:check:test`: 임의의 잘못된 색상·타이포·폰트·폭 토큰을 넣었을 때 검사 실패를 확인한다.
- 컴포넌트 테스트는 접근 가능한 이름, 선택·입력의 전달, 취소·적용, 모달 focus 동작을 검증한다. 소스에 특정 클래스 문자열이 있는지만 검사하지 않는다.
- 통합 검증은 `npm test`, `npm run build`, E2E와 데스크톱·모바일 스크린샷으로 확인한다. 기본 시뮬레이션, 백테스트, 설정 Sheet, 긴 이름과 큰 숫자를 포함한다.
- 글자 가독성, 44px 조작 영역, 입력 정렬, 페이지 가로 넘침, 모달 닫기 접근성을 화면에서 확인하고 QA 기록에 남긴다.

토큰 검사는 정적인 완성형 클래스만 대상으로 한다. `text-${size}`처럼 클래스 이름을 조립하지 말고 완전한 클래스 문자열을 매핑한다. 사용자 정의 `.ui-*` 컴포넌트 클래스와 차트 애니메이션 훅은 유틸리티 토큰 검사의 대상이 아니며, CSS 빌드와 화면 검증으로 확인한다.

### 비교 종목 선택

선택 종목은 최대 3개이며 최소 1개를 유지한다. 선택 영역에서 기준 종목을 명시하고, 각 종목에 제거 버튼과 비교 종목의 `기준으로 설정` 동작을 제공한다. 기준을 제거하면 남은 첫 종목을 기준으로 승격한다. 기준 변경은 기존 날짜·공통 데이터 검증을 함께 적용한다.

레버리지 가족은 현재 선택을 상품 묶음으로 대체하는 단축 선택이다. 선택된 가족을 다시 누르면 현재 기준 종목만 남긴다. 개별 목록에는 선택된 종목도 남겨 `aria-pressed`와 체크 표시를 제공하며, 다시 누르면 해제한다. 3개 제한에 도달해도 선택된 종목의 해제는 막지 않는다.
